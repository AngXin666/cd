# 🔧 JS文件上传问题解决方案

## 问题
js文件夹上传不进去

## 解决方案

### 方案1：逐个文件上传（推荐）

1. 打开：https://supabase.com/dashboard/project/wxvrwkpkioalqdsfswwu/storage/buckets/h5-app

2. 进入 `v1.0.0` 文件夹

3. **先创建 js 文件夹**：
   - 点击 "New folder"
   - 输入：`js`
   - 点击 "Create"

4. **进入 js 文件夹**

5. **上传文件**：
   - 点击 "Upload files"
   - 选择 `dist\js\vendors.D6XJLh3d.js` 文件
   - 上传

6. **返回 v1.0.0 文件夹**

7. **创建 static 文件夹并上传**：
   - 点击 "New folder" → 输入 `static` → 创建
   - 进入 static 文件夹
   - 点击 "New folder" → 输入 `images` → 创建
   - 进入 images 文件夹
   - 上传 `dist\static\images\` 下的所有图片

---

### 方案2：使用Supabase CLI上传（如果你有CLI）

```powershell
# 上传整个dist文件夹
supabase storage cp dist/* h5-app/v1.0.0/ --recursive
```

---

### 方案3：使用PowerShell脚本上传（自动化）

我可以帮你创建一个脚本，使用Supabase API自动上传所有文件。

需要吗？

---

## 最终文件结构应该是：

```
h5-app/
└── v1.0.0/
    ├── js/
    │   └── vendors.D6XJLh3d.js
    └── static/
        └── images/
            ├── profile.png
            └── workspace.png
```

---

## 验证上传成功

上传完成后，访问这个URL应该能下载文件：
https://wxvrwkpkioalqdsfswwu.supabase.co/storage/v1/object/public/h5-app/v1.0.0/js/vendors.D6XJLh3d.js

如果能访问，说明上传成功！

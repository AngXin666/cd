# 通知系统多租户支持检查报告

## 检查日期
2025-11-05

## 数据库架构目标

```
├── public Schema（中央管理）
│   ├── tenants（租户表）
│   ├── profiles（中央管理员）
│   ├── notifications（❌ 当前存在，应该移除或仅用于中央管理员）
│   └── auth.users（所有用户认证）
│
├── tenant_test1 Schema（租户1）
│   ├── profiles（租户1的用户）
│   └── notifications（✅ 应该存在，租户1的通知）
│
└── tenant_test2 Schema（租户2）
    ├── profiles（租户2的用户）
    └── notifications（✅ 应该存在，租户2的通知）
```

## 检查结果

### ❌ 通知系统不支持多租户架构

#### 问题 1：数据库表结构
**当前状态**：
- ✅ 租户 Schema 创建函数 `create_notifications_table()` 已存在
- ✅ 每个租户 Schema 都应该有 notifications 表
- ❌ 但是 public Schema 也有 notifications 表

**问题**：
- 所有通知都存储在 `public.notifications`
- 租户之间的通知没有隔离
- 租户1可以看到租户2的通知（如果查询不当）

#### 问题 2：API 函数直接操作 public.notifications

**getNotifications() 函数**：
```typescript
export async function getNotifications(userId: string, limit = 50): Promise<Notification[]> {
  const {data, error} = await supabase
    .from('notifications')  // ❌ 直接查询 public.notifications
    .select('*')
    .eq('recipient_id', userId)
    .order('created_at', {ascending: false})
    .limit(limit)
  // ...
}
```

**问题**：
- 直接查询 `public.notifications`
- 不支持租户 Schema
- 无法实现租户数据隔离

**createNotification() 函数**：
```typescript
export async function createNotification(notification: {...}): Promise<string | null> {
  // 使用 create_notifications_batch RPC 函数
  const {data, error} = await supabase.rpc('create_notifications_batch', {
    notifications: [...]
  })
  // ...
}
```

**问题**：
- `create_notifications_batch` RPC 函数操作 `public.notifications`
- 不支持租户 Schema
- 所有租户的通知混在一起

#### 问题 3：RPC 函数 create_notifications_batch

**当前实现**：
```sql
CREATE OR REPLACE FUNCTION create_notifications_batch(notifications jsonb)
RETURNS integer
AS $$
BEGIN
  -- 插入通知
  INSERT INTO notifications (...)  -- ❌ 插入到 public.notifications
  SELECT ...
  FROM jsonb_array_elements(notifications) AS n
  RETURNING 1
  -- ...
END;
$$;
```

**问题**：
- 固定插入到 `public.notifications`
- 不支持动态选择租户 Schema
- 无法实现租户数据隔离

#### 问题 4：其他通知相关函数

**getUnreadNotificationCount() 函数**：
```typescript
export async function getUnreadNotificationCount(userId: string): Promise<number> {
  const {count, error} = await supabase
    .from('notifications')  // ❌ 直接查询 public.notifications
    .select('*', {count: 'exact', head: true})
    .eq('recipient_id', userId)
    .eq('is_read', false)
  // ...
}
```

**问题**：同样直接操作 `public.notifications`

## 影响分析

### 数据隔离问题
- ❌ 租户1的通知和租户2的通知存储在同一个表
- ❌ 如果查询条件不当，可能泄露其他租户的通知
- ❌ 无法为不同租户设置不同的通知策略

### 安全性问题
- ❌ RLS 策略基于 `public.notifications`
- ❌ 无法实现租户级别的权限控制
- ❌ 可能存在跨租户访问风险

### 扩展性问题
- ❌ 所有租户的通知在一个表，数据量大时性能下降
- ❌ 无法为不同租户设置不同的通知保留策略
- ❌ 无法独立备份或恢复某个租户的通知数据

## 修复方案

### 方案 1：创建租户通知 RPC 函数（推荐）

#### 步骤 1：创建 get_tenant_notifications RPC 函数
```sql
CREATE FUNCTION public.get_tenant_notifications(
  p_tenant_id uuid,
  p_user_id uuid,
  p_limit int DEFAULT 50
)
RETURNS jsonb
AS $$
DECLARE
  v_schema_name text;
  v_sql text;
  v_result jsonb;
BEGIN
  -- 获取租户的 Schema 名称
  SELECT schema_name INTO v_schema_name
  FROM public.tenants
  WHERE id = p_tenant_id;

  IF v_schema_name IS NULL THEN
    RAISE EXCEPTION 'Tenant not found: %', p_tenant_id;
  END IF;

  -- 从租户 Schema 查询通知
  v_sql := format('
    SELECT jsonb_agg(row_to_json(n.*))
    FROM (
      SELECT *
      FROM %I.notifications
      WHERE receiver_id = $1
      ORDER BY created_at DESC
      LIMIT $2
    ) n
  ', v_schema_name);

  EXECUTE v_sql INTO v_result USING p_user_id, p_limit;

  RETURN COALESCE(v_result, '[]'::jsonb);
END;
$$;
```

#### 步骤 2：创建 create_tenant_notification RPC 函数
```sql
CREATE FUNCTION public.create_tenant_notification(
  p_tenant_id uuid,
  p_sender_id uuid,
  p_receiver_id uuid,
  p_title text,
  p_content text,
  p_type text DEFAULT 'user'
)
RETURNS jsonb
AS $$
DECLARE
  v_schema_name text;
  v_sql text;
  v_result jsonb;
BEGIN
  -- 获取租户的 Schema 名称
  SELECT schema_name INTO v_schema_name
  FROM public.tenants
  WHERE id = p_tenant_id;

  IF v_schema_name IS NULL THEN
    RAISE EXCEPTION 'Tenant not found: %', p_tenant_id;
  END IF;

  -- 在租户 Schema 创建通知
  v_sql := format('
    INSERT INTO %I.notifications (sender_id, receiver_id, title, content, type)
    VALUES ($1, $2, $3, $4, $5)
    RETURNING row_to_json(%I.notifications.*)
  ', v_schema_name, v_schema_name);

  EXECUTE v_sql INTO v_result USING p_sender_id, p_receiver_id, p_title, p_content, p_type;

  RETURN v_result;
END;
$$;
```

#### 步骤 3：创建 mark_tenant_notification_read RPC 函数
```sql
CREATE FUNCTION public.mark_tenant_notification_read(
  p_tenant_id uuid,
  p_notification_id uuid,
  p_user_id uuid
)
RETURNS boolean
AS $$
DECLARE
  v_schema_name text;
  v_sql text;
BEGIN
  -- 获取租户的 Schema 名称
  SELECT schema_name INTO v_schema_name
  FROM public.tenants
  WHERE id = p_tenant_id;

  IF v_schema_name IS NULL THEN
    RAISE EXCEPTION 'Tenant not found: %', p_tenant_id;
  END IF;

  -- 更新通知状态
  v_sql := format('
    UPDATE %I.notifications
    SET status = ''read'', read_at = NOW()
    WHERE id = $1 AND receiver_id = $2
  ', v_schema_name);

  EXECUTE v_sql USING p_notification_id, p_user_id;

  RETURN true;
END;
$$;
```

#### 步骤 4：重写前端 API 函数

**getNotifications() 函数**：
```typescript
export async function getNotifications(userId: string, limit = 50): Promise<Notification[]> {
  try {
    console.log('🔍 getNotifications: 开始获取通知列表')

    // 获取当前用户的租户信息
    const {
      data: {user}
    } = await supabase.auth.getUser()

    if (!user) {
      console.error('❌ 用户未登录')
      return []
    }

    const tenantId = user.user_metadata?.tenant_id

    console.log('👤 当前用户:')
    console.log('  - 用户 ID:', user.id)
    console.log('  - 租户 ID:', tenantId || '无（中央管理员）')

    // 如果是租户用户，使用 RPC 函数查询租户 Schema
    if (tenantId) {
      console.log('  - 目标：租户 Schema')
      console.log('  - 使用函数: get_tenant_notifications')

      const {data, error} = await supabase.rpc('get_tenant_notifications', {
        p_tenant_id: tenantId,
        p_user_id: userId,
        p_limit: limit
      })

      if (error) {
        console.error('❌ 获取租户通知失败:', error)
        return []
      }

      console.log(`✅ 获取到 ${data?.length || 0} 条通知`)
      return Array.isArray(data) ? data : []
    }

    // 否则是中央管理员，查询 public.notifications
    console.log('  - 目标：public.notifications')
    console.log('  - 当前用户是中央管理员')

    const {data, error} = await supabase
      .from('notifications')
      .select('*')
      .eq('recipient_id', userId)
      .order('created_at', {ascending: false})
      .limit(limit)

    if (error) {
      console.error('❌ 获取通知失败:', error)
      return []
    }

    console.log(`✅ 获取到 ${data?.length || 0} 条通知`)
    return Array.isArray(data) ? data : []
  } catch (error) {
    console.error('❌ 获取通知异常:', error)
    return []
  }
}
```

**createNotification() 函数**：
```typescript
export async function createNotification(notification: {
  receiver_id: string
  title: string
  content: string
  type?: string
}): Promise<string | null> {
  try {
    console.log('📤 createNotification: 开始创建通知')

    // 获取当前用户信息
    const {
      data: {user}
    } = await supabase.auth.getUser()

    if (!user) {
      console.error('❌ 用户未登录')
      return null
    }

    const tenantId = user.user_metadata?.tenant_id
    const senderId = user.id

    console.log('👤 发送者信息:')
    console.log('  - 用户 ID:', senderId)
    console.log('  - 租户 ID:', tenantId || '无（中央管理员）')

    // 如果是租户用户，使用 RPC 函数在租户 Schema 创建
    if (tenantId) {
      console.log('  - 目标：租户 Schema')
      console.log('  - 使用函数: create_tenant_notification')

      const {data, error} = await supabase.rpc('create_tenant_notification', {
        p_tenant_id: tenantId,
        p_sender_id: senderId,
        p_receiver_id: notification.receiver_id,
        p_title: notification.title,
        p_content: notification.content,
        p_type: notification.type || 'user'
      })

      if (error) {
        console.error('❌ 创建租户通知失败:', error)
        return null
      }

      console.log('✅ 租户通知创建成功')
      return data?.id || null
    }

    // 否则是中央管理员，在 public.notifications 创建
    console.log('  - 目标：public.notifications')
    console.log('  - 当前用户是中央管理员')

    const {data, error} = await supabase
      .from('notifications')
      .insert({
        sender_id: senderId,
        receiver_id: notification.receiver_id,
        title: notification.title,
        content: notification.content,
        type: notification.type || 'user'
      })
      .select()
      .single()

    if (error) {
      console.error('❌ 创建通知失败:', error)
      return null
    }

    console.log('✅ 通知创建成功')
    return data?.id || null
  } catch (error) {
    console.error('❌ 创建通知异常:', error)
    return null
  }
}
```

### 方案 2：统一使用 public.notifications（不推荐）

**优点**：
- 实现简单，不需要修改现有代码
- 查询性能较好（单表查询）

**缺点**：
- ❌ 无法实现真正的租户数据隔离
- ❌ 需要在每个查询中添加 tenant_id 过滤
- ❌ 存在数据泄露风险
- ❌ 无法为不同租户设置不同的策略

**不推荐原因**：
- 与系统的多租户架构设计不一致
- 存在安全隐患
- 扩展性差

## 修复优先级

### 高优先级（影响数据安全）
1. ⚠️ 创建 `get_tenant_notifications` RPC 函数
2. ⚠️ 创建 `create_tenant_notification` RPC 函数
3. ⚠️ 重写 `getNotifications()` 函数
4. ⚠️ 重写 `createNotification()` 函数

### 中优先级（完善功能）
5. 创建 `mark_tenant_notification_read` RPC 函数
6. 创建 `get_tenant_unread_count` RPC 函数
7. 重写 `getUnreadNotificationCount()` 函数
8. 重写 `markNotificationAsRead()` 函数

### 低优先级（优化和清理）
9. 清理 `public.notifications` 表（如果不需要）
10. 更新文档和注释
11. 添加单元测试

## 验证计划

### 测试场景 1：租户1用户查看通知
**预期**：只能看到租户1的通知

**步骤**：
1. 以租户1老板身份登录
2. 调用 `getNotifications()`
3. 验证返回的通知都属于租户1

### 测试场景 2：租户2用户查看通知
**预期**：只能看到租户2的通知

**步骤**：
1. 以租户2老板身份登录
2. 调用 `getNotifications()`
3. 验证返回的通知都属于租户2

### 测试场景 3：租户1用户发送通知
**预期**：通知创建在租户1 Schema

**步骤**：
1. 以租户1老板身份登录
2. 调用 `createNotification()`
3. 验证通知创建在 `tenant_test1.notifications`

### 测试场景 4：跨租户通知隔离
**预期**：租户1用户无法看到租户2的通知

**步骤**：
1. 在租户2创建一条通知
2. 以租户1用户身份查询通知
3. 验证无法看到租户2的通知

## 总结

### 当前状态
- ❌ 通知系统不支持多租户架构
- ❌ 所有通知存储在 `public.notifications`
- ❌ 存在数据泄露风险
- ❌ 无法实现租户级别的数据隔离

### 需要修复的内容
1. 创建租户通知 RPC 函数（3个）
2. 重写前端 API 函数（4个）
3. 更新通知类型定义
4. 添加测试验证

### 修复后的效果
- ✅ 每个租户的通知独立存储
- ✅ 租户之间数据完全隔离
- ✅ 支持租户级别的权限控制
- ✅ 与系统多租户架构一致

### 建议
**强烈建议立即修复通知系统的多租户支持**，因为：
1. 当前存在数据安全隐患
2. 与系统整体架构不一致
3. 修复成本相对较低
4. 影响用户体验和数据隐私

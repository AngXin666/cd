# 实现计划：仓库切换无感优化

## 概述

本实现计划将仓库切换优化功能分解为一系列增量式的编码任务。每个任务都基于前一个任务构建，最终实现完整的无感切换功能。

## 任务

- [x] 1. 创建核心 Composable 和类型定义
  - 创建 `composables/useWarehouseDataCache.ts` 文件
  - 定义 TypeScript 接口：`CacheItem`, `UseWarehouseDataCacheOptions`, `UseWarehouseDataCacheReturn`
  - 实现基础的缓存存储结构（使用 `Map`）
  - 实现缓存过期检测函数 `isCacheExpired`
  - 实现缓存检查函数 `isCached`
  - _需求：1.2, 3.4_

- [ ]* 1.1 为核心类型定义编写单元测试
  - 测试缓存过期检测逻辑
  - 测试缓存检查逻辑
  - _需求：1.2, 3.4_

- [x] 2. 实现单个仓库数据加载功能
  - [x] 2.1 实现 `loadWarehouse` 函数
    - 接收仓库 ID 和静默模式参数
    - 调用 `loadDataFn` 加载数据
    - 更新缓存状态（loading, data, timestamp, error）
    - 处理加载错误并记录日志
    - _需求：1.1, 1.3, 6.1_

  - [ ]* 2.2 编写属性测试：加载成功更新缓存
    - **属性 1：预加载完整性（部分）**
    - 测试加载成功后数据被正确缓存
    - **验证：需求 1.1, 1.2**

  - [ ]* 2.3 编写属性测试：加载失败记录错误
    - **属性 2：预加载容错性（部分）**
    - 测试加载失败时错误被记录且不影响其他操作
    - **验证：需求 1.3, 6.1**

- [x] 3. 实现数据预加载功能
  - [x] 3.1 实现 `preloadAll` 函数
    - 并发加载所有仓库数据（使用 `Promise.allSettled`）
    - 更新预加载进度 `preloadedCount`
    - 使用静默模式加载（不显示错误提示）
    - 记录预加载完成日志
    - _需求：1.1, 1.2, 1.3, 5.4_

  - [ ]* 3.2 编写属性测试：预加载完整性
    - **属性 1：预加载完整性**
    - 测试预加载后所有仓库数据都在缓存中
    - **验证：需求 1.1, 1.2**

  - [ ]* 3.3 编写属性测试：预加载容错性
    - **属性 2：预加载容错性**
    - 测试某个仓库失败时其他仓库继续加载
    - **验证：需求 1.3, 6.1**

  - [ ]* 3.4 编写属性测试：预加载完成日志记录
    - **属性 11：预加载完成日志记录**
    - 测试预加载完成时记录日志但不显示 UI 提示
    - **验证：需求 5.4**

- [x] 4. 实现仓库切换功能
  - [x] 4.1 实现 `switchWarehouse` 函数
    - 检查目标仓库是否已缓存
    - 缓存命中：直接切换索引，不显示加载状态
    - 缓存未命中：显示加载状态，加载数据后切换
    - 更新 `currentIndex`
    - _需求：1.4, 2.1, 2.2, 2.3, 2.4_

  - [ ]* 4.2 编写属性测试：缓存命中时无加载状态
    - **属性 3：缓存命中时无加载状态**
    - 测试切换到已缓存仓库时立即显示数据且不显示加载状态
    - **验证：需求 2.1, 2.3, 4.2**

  - [ ]* 4.3 编写属性测试：缓存未命中时显示局部加载
    - **属性 4：缓存未命中时显示局部加载**
    - 测试切换到未缓存仓库时显示局部加载状态
    - **验证：需求 2.2, 4.3**

  - [ ]* 4.4 编写属性测试：切换后索引更新
    - **属性 5：切换后索引更新**
    - 测试切换后当前索引被正确更新
    - **验证：需求 2.4**

- [x] 5. 实现缓存更新和刷新功能
  - [x] 5.1 实现 `refreshCurrent` 函数
    - 重新加载当前仓库数据
    - 显示加载状态
    - _需求：3.2_

  - [x] 5.2 实现 `refreshAll` 函数
    - 清空所有缓存
    - 先加载当前仓库
    - 后台预加载其他仓库
    - _需求：3.2_

  - [x] 5.3 实现 `clearCache` 函数
    - 清空缓存 Map
    - 重置预加载计数
    - _需求：3.2_

  - [ ]* 5.4 编写属性测试：手动刷新清空缓存
    - **属性 7：手动刷新清空缓存**
    - 测试刷新时清空缓存并重新加载
    - **验证：需求 3.2**

- [x] 6. 实现缓存过期和后台更新
  - [x] 6.1 实现 `updateExpiredCache` 函数
    - 查找所有过期的缓存项
    - 在后台静默更新过期数据
    - 不显示加载状态
    - _需求：3.4, 4.4_

  - [x] 6.2 在 `onMounted` 中设置定时器
    - 每 1 分钟检查并更新过期缓存
    - 在 `onUnmounted` 中清理定时器
    - _需求：3.4_

  - [ ]* 6.3 编写属性测试：缓存过期触发后台更新
    - **属性 9：缓存过期触发后台更新**
    - 测试缓存过期时后台更新且不显示加载状态
    - **验证：需求 3.4, 4.4**

- [x] 7. 实现初始化和加载顺序优化
  - [x] 7.1 完善 `onMounted` 初始化逻辑
    - 先加载当前仓库（显示加载状态）
    - 延迟 500ms 后台预加载其他仓库
    - 设置缓存过期检查定时器
    - _需求：5.1, 5.2_

  - [ ]* 7.2 编写属性测试：加载顺序优先级
    - **属性 10：加载顺序优先级**
    - 测试当前仓库先加载，其他仓库后加载
    - **验证：需求 5.1, 5.2**

- [x] 8. 实现计算属性和辅助方法
  - [x] 8.1 实现 `currentData` 计算属性
    - 根据当前索引从缓存中获取数据
    - _需求：2.1_

  - [x] 8.2 实现 `preloadProgress` 计算属性
    - 计算预加载进度百分比
    - _需求：1.1_

  - [x] 8.3 实现 `getWarehouseData` 方法
    - 获取指定仓库的缓存数据
    - _需求：1.4_

- [x] 9. Checkpoint - 确保 Composable 测试通过
  - 运行所有单元测试和属性测试
  - 确保所有测试通过
  - 如有问题，询问用户

- [x] 10. 迁移老板首页
  - [x] 10.1 定义 `BossHomeData` 类型
    - 包含 stats 和 driverStats
    - _需求：7.1_

  - [x] 10.2 创建 `loadBossHomeData` 函数
    - 并发加载统计数据和司机状态
    - _需求：7.1_

  - [x] 10.3 集成 `useWarehouseDataCache`
    - 替换原有的 `loadData` 逻辑
    - 使用 composable 提供的状态和方法
    - _需求：7.1_

  - [x] 10.4 更新 `handleWarehouseChange` 函数
    - 调用 `switchWarehouse` 而不是 `loadData`
    - _需求：7.1_

  - [x] 10.5 更新下拉刷新逻辑
    - 调用 `refreshAll` 方法
    - _需求：7.1_

  - [ ]* 10.6 编写集成测试
    - 测试老板首页的完整切换流程
    - 测试预加载流程
    - _需求：7.1_

- [x] 11. 迁移车队长首页
  - [x] 11.1 定义 `ManagerHomeData` 类型
    - _需求：7.2_

  - [x] 11.2 创建 `loadManagerHomeData` 函数
    - _需求：7.2_

  - [x] 11.3 集成 `useWarehouseDataCache`
    - _需求：7.2_

  - [x] 11.4 更新仓库切换和刷新逻辑
    - _需求：7.2_

  - [ ]* 11.5 编写集成测试
    - _需求：7.2_

- [x] 12. 迁移司机首页
  - [x] 12.1 定义 `DriverHomeData` 类型
    - _需求：7.3_

  - [x] 12.2 创建 `loadDriverHomeData` 函数
    - _需求：7.3_

  - [x] 12.3 集成 `useWarehouseDataCache`
    - _需求：7.3_

  - [x] 12.4 更新仓库切换和刷新逻辑
    - _需求：7.3_

  - [ ]* 12.5 编写集成测试
    - _需求：7.3_

- [x] 13. 迁移司机管理页面
  - [x] 13.1 定义 `DriverManagementData` 类型
    - _需求：7.4_

  - [x] 13.2 创建 `loadDriverManagementData` 函数
    - _需求：7.4_

  - [x] 13.3 集成 `useWarehouseDataCache`
    - _需求：7.4_

  - [x] 13.4 更新仓库切换和刷新逻辑
    - _需求：7.4_

  - [x] 13.5 编写集成测试
    - _需求：7.4_

- [x] 14. 迁移用户管理页面
  - [x] 14.1 定义 `UserManagementData` 类型
    - _需求：7.5_

  - [x] 14.2 创建 `loadUserManagementData` 函数
    - _需求：7.5_

  - [x] 14.3 集成 `useWarehouseDataCache`
    - _需求：7.5_

  - [x] 14.4 更新仓库切换和刷新逻辑
    - _需求：7.5_

  - [ ]* 14.5 编写集成测试
    - _需求：7.5_

- [x] 15. 实现 SSE 集成
  - [x] 15.1 在各页面中监听仓库分配更新事件
    - 调用 `refreshAll` 重新加载数据
    - _需求：3.3_

  - [ ]* 15.2 编写属性测试：仓库分配变更触发重新加载
    - **属性 8：仓库分配变更触发重新加载**
    - 测试收到分配变更事件时重新加载数据
    - **验证：需求 3.3**

- [x] 16. 实现错误处理 UI
  - [x] 16.1 在页面中添加错误提示组件
    - 检测当前仓库是否有错误
    - 显示错误信息和重试按钮
    - _需求：6.2_

  - [x] 16.2 实现重试功能
    - 点击重试按钮调用 `refreshCurrent`
    - _需求：6.3_

  - [ ]* 16.3 编写属性测试：错误仓库显示重试选项
    - **属性 12：错误仓库显示重试选项**
    - 测试切换到失败仓库时显示错误提示和重试按钮
    - **验证：需求 6.2**

  - [ ]* 16.4 编写属性测试：重试触发重新加载
    - **属性 13：重试触发重新加载**
    - 测试点击重试时重新加载数据
    - **验证：需求 6.3**

- [x] 17. 最终 Checkpoint - 完整测试
  - 运行所有单元测试、属性测试和集成测试
  - 在各个页面手动测试切换体验
  - 验证无"加载中"闪动
  - 确保所有测试通过
  - 如有问题，询问用户

- [x] 18. 文档和清理
  - [x] 18.1 更新相关文档
    - 更新 README 说明新的缓存机制
    - 添加迁移指南到项目文档
    - _需求：所有_

  - [x] 18.2 清理旧代码
    - 删除各页面中废弃的 `loadData` 函数
    - 删除手动管理的 loading 状态
    - 清理未使用的导入
    - _需求：所有_

## 注意事项

- 每个任务完成后停止，等待用户确认再继续
- 标记 `*` 的任务为可选测试任务，可以跳过以加快 MVP 开发
- 属性测试使用 fast-check 库，每个测试至少运行 100 次
- 所有代码必须包含 JSDoc 注释
- 遵循项目的 TypeScript 编码规范

# 账号类型和权限体系

## 📋 账号类型概述

系统支持四种账号类型，每种账号拥有不同的权限和职责。

---

## 👥 账号类型详解

### 1. 超级管理员（super_admin）

**定位**：中央管理系统管理员

**职责**：
- 管理所有租户（老板账号）
- 管理租户配置
- 监控系统运行状态

**权限**：
- ✅ 创建老板账号
- ✅ 修改老板账号信息
- ✅ 暂停老板账号
- ✅ 删除老板账号
- ✅ 查看所有租户配置
- ✅ 管理租户数据库配置
- ❌ 不能直接操作租户内部的业务数据

**登录入口**：中央管理系统

**默认账号**：
- 用户名：admin@fleet.com
- 密码：hye19911206

---

### 2. 老板（boss）

**定位**：租户系统的最高权限所有者

**职责**：
- 管理自己租户内的所有数据
- 管理平级账号
- 管理车队长账号
- 管理司机账号

**权限**：
- ✅ 查看所有数据
- ✅ 创建、修改、删除所有数据
- ✅ 创建平级账号（最多3个）
- ✅ 授予平级账号权限
- ✅ 创建车队长账号
- ✅ 授予车队长权限和管辖范围
- ✅ 创建司机账号
- ✅ 修改、删除司机账号

**登录入口**：租户系统

---

### 3. 平级账号（peer）

**定位**：与老板平级的协作账号

**职责**：
- 根据老板授予的权限协助管理租户
- 最多可创建3个平级账号

**权限类型**：

#### 3.1 完整权限（full_permission）
- ✅ 拥有与老板相同的所有权限
- ✅ 查看所有数据
- ✅ 创建、修改、删除所有数据
- ✅ 管理车队长账号
- ✅ 管理司机账号
- ❌ 不能创建或管理其他平级账号

#### 3.2 只读权限（read_only）
- ✅ 查看所有数据
- ❌ 不能创建、修改、删除数据
- ❌ 不能管理任何账号

**登录入口**：租户系统

---

### 4. 车队长（manager）

**定位**：管理指定范围内的车队和司机

**职责**：
- 管理分配给自己的车队
- 管理管辖范围内的司机
- 处理日常运营事务

**权限类型**：

#### 4.1 完整权限（full_permission）
- ✅ 查看管辖范围内的所有数据
- ✅ 创建、修改、删除管辖范围内的数据
- ✅ 创建司机账号
- ✅ 修改、删除管辖范围内的司机账号
- ✅ 审批请假申请
- ✅ 管理考勤记录
- ❌ 不能访问管辖范围外的数据

#### 4.2 只读权限（read_only）
- ✅ 查看管辖范围内的所有数据
- ❌ 不能创建、修改、删除数据
- ❌ 不能管理司机账号

**管辖范围**：
- 由老板或拥有完整权限的平级账号指定
- 可以管理多个仓库
- 可以管理多个车队
- 只能管理分配给自己的司机

**登录入口**：租户系统

---

### 5. 司机（driver）

**定位**：基层操作人员

**职责**：
- 完成日常工作任务
- 记录工作数据
- 提交请假申请

**权限**：
- ✅ 查看自己的信息
- ✅ 修改自己的基本信息（部分字段）
- ✅ 打卡签到
- ✅ 提交计件工作记录
- ✅ 申请请假
- ✅ 查看自己的考勤记录
- ✅ 查看自己的工资记录
- ❌ 不能查看其他司机的数据
- ❌ 不能修改系统配置

**登录入口**：租户系统

---

## 🔐 权限矩阵

| 功能 | 超级管理员 | 老板 | 平级账号（完整） | 平级账号（只读） | 车队长（完整） | 车队长（只读） | 司机 |
|------|-----------|------|----------------|----------------|--------------|--------------|------|
| 管理租户配置 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 创建老板账号 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 修改老板账号 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 暂停老板账号 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 删除老板账号 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 创建平级账号 | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 授予平级权限 | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 创建车队长 | ❌ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| 授予车队长权限 | ❌ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| 创建司机 | ❌ | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |
| 修改司机（全部） | ❌ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| 修改司机（管辖范围） | ❌ | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |
| 删除司机（全部） | ❌ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| 删除司机（管辖范围） | ❌ | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |
| 查看所有数据 | ❌ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| 查看管辖范围数据 | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| 查看自己的数据 | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 修改所有数据 | ❌ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| 修改管辖范围数据 | ❌ | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |
| 修改自己的数据 | ❌ | ✅ | ✅ | ❌ | ✅ | ❌ | ✅ |
| 审批请假 | ❌ | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |
| 申请请假 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| 打卡签到 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| 提交计件工作 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |

---

## 🏗️ 数据库设计

### profiles 表字段

```sql
CREATE TABLE profiles (
  id uuid PRIMARY KEY,
  tenant_id uuid REFERENCES tenant_configs(id),  -- 所属租户
  role text NOT NULL,  -- 角色：super_admin, boss, peer, manager, driver
  permission_level text,  -- 权限级别：full_permission, read_only
  boss_id uuid REFERENCES profiles(id),  -- 所属老板（仅平级账号和车队长）
  manager_id uuid REFERENCES profiles(id),  -- 所属车队长（仅司机）
  managed_warehouses uuid[],  -- 管辖的仓库（仅车队长）
  real_name text,
  phone text,
  email text,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
```

### 字段说明

- **tenant_id**：所属租户ID，超级管理员为 NULL
- **role**：角色类型
  - `super_admin`：超级管理员
  - `boss`：老板
  - `peer`：平级账号
  - `manager`：车队长
  - `driver`：司机
- **permission_level**：权限级别（仅平级账号和车队长）
  - `full_permission`：完整权限
  - `read_only`：只读权限
- **boss_id**：所属老板ID（仅平级账号和车队长需要）
- **manager_id**：所属车队长ID（仅司机需要）
- **managed_warehouses**：管辖的仓库ID数组（仅车队长需要）

---

## 🔄 账号创建流程

### 1. 超级管理员创建老板账号

```typescript
// 超级管理员操作
const boss = await createBossAccount({
  tenant_id: tenantId,
  real_name: '张三',
  phone: '13800000001',
  email: 'zhangsan@example.com',
  password: 'password123'
})
```

### 2. 老板创建平级账号

```typescript
// 老板操作
const peer = await createPeerAccount({
  boss_id: bossId,
  real_name: '李四',
  phone: '13800000002',
  email: 'lisi@example.com',
  password: 'password123',
  permission_level: 'full_permission'  // 或 'read_only'
})
```

### 3. 老板或平级账号（完整权限）创建车队长

```typescript
// 老板或平级账号（完整权限）操作
const manager = await createManagerAccount({
  boss_id: bossId,
  real_name: '王五',
  phone: '13800000003',
  email: 'wangwu@example.com',
  password: 'password123',
  permission_level: 'full_permission',  // 或 'read_only'
  managed_warehouses: [warehouse1Id, warehouse2Id]
})
```

### 4. 老板、平级账号（完整权限）或车队长（完整权限）创建司机

```typescript
// 老板、平级账号（完整权限）或车队长（完整权限）操作
const driver = await createDriverAccount({
  boss_id: bossId,
  manager_id: managerId,  // 可选
  real_name: '赵六',
  phone: '13800000004',
  password: 'password123'
})
```

---

## 🛡️ RLS 策略设计

### 超级管理员策略

```sql
-- 超级管理员可以查看所有租户配置
CREATE POLICY "超级管理员查看租户配置" ON tenant_configs
  FOR SELECT
  TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM profiles
      WHERE profiles.id = auth.uid()
      AND profiles.role = 'super_admin'
    )
  );
```

### 老板策略

```sql
-- 老板可以查看自己租户内的所有数据
CREATE POLICY "老板查看所有数据" ON profiles
  FOR SELECT
  TO authenticated
  USING (
    tenant_id = (
      SELECT tenant_id FROM profiles
      WHERE id = auth.uid()
      AND role = 'boss'
    )
  );
```

### 平级账号策略

```sql
-- 平级账号（完整权限）可以查看和修改租户内的所有数据
CREATE POLICY "平级账号完整权限" ON profiles
  FOR ALL
  TO authenticated
  USING (
    tenant_id = (
      SELECT tenant_id FROM profiles
      WHERE id = auth.uid()
      AND role = 'peer'
      AND permission_level = 'full_permission'
    )
  );

-- 平级账号（只读）只能查看数据
CREATE POLICY "平级账号只读权限" ON profiles
  FOR SELECT
  TO authenticated
  USING (
    tenant_id = (
      SELECT tenant_id FROM profiles
      WHERE id = auth.uid()
      AND role = 'peer'
      AND permission_level = 'read_only'
    )
  );
```

### 车队长策略

```sql
-- 车队长（完整权限）可以查看和修改管辖范围内的数据
CREATE POLICY "车队长完整权限" ON drivers
  FOR ALL
  TO authenticated
  USING (
    warehouse_id = ANY(
      SELECT unnest(managed_warehouses) FROM profiles
      WHERE id = auth.uid()
      AND role = 'manager'
      AND permission_level = 'full_permission'
    )
  );

-- 车队长（只读）只能查看管辖范围内的数据
CREATE POLICY "车队长只读权限" ON drivers
  FOR SELECT
  TO authenticated
  USING (
    warehouse_id = ANY(
      SELECT unnest(managed_warehouses) FROM profiles
      WHERE id = auth.uid()
      AND role = 'manager'
      AND permission_level = 'read_only'
    )
  );
```

### 司机策略

```sql
-- 司机只能查看和修改自己的数据
CREATE POLICY "司机查看自己的数据" ON profiles
  FOR SELECT
  TO authenticated
  USING (
    id = auth.uid()
    AND role = 'driver'
  );

CREATE POLICY "司机修改自己的数据" ON profiles
  FOR UPDATE
  TO authenticated
  USING (
    id = auth.uid()
    AND role = 'driver'
  );
```

---

## 📱 界面设计

### 超级管理员界面

- 租户配置管理
- 老板账号管理
- 系统监控

### 老板界面

- 仪表盘
- 平级账号管理
- 车队长管理
- 司机管理
- 车辆管理
- 仓库管理
- 考勤管理
- 工资管理
- 报表统计

### 平级账号界面（完整权限）

- 与老板界面相同（除了平级账号管理）

### 平级账号界面（只读）

- 仪表盘（只读）
- 数据查看（只读）
- 报表查看（只读）

### 车队长界面（完整权限）

- 仪表盘（管辖范围）
- 司机管理（管辖范围）
- 车辆管理（管辖范围）
- 考勤管理（管辖范围）
- 请假审批（管辖范围）

### 车队长界面（只读）

- 仪表盘（管辖范围，只读）
- 数据查看（管辖范围，只读）

### 司机界面

- 个人信息
- 打卡签到
- 计件工作提交
- 请假申请
- 考勤记录查看
- 工资记录查看

---

## 🔍 权限检查函数

### 检查是否为超级管理员

```typescript
export async function isSuperAdmin(userId: string): Promise<boolean> {
  const { data } = await supabase
    .from('profiles')
    .select('role')
    .eq('id', userId)
    .single()
  
  return data?.role === 'super_admin'
}
```

### 检查是否为老板

```typescript
export async function isBoss(userId: string): Promise<boolean> {
  const { data } = await supabase
    .from('profiles')
    .select('role')
    .eq('id', userId)
    .single()
  
  return data?.role === 'boss'
}
```

### 检查是否拥有完整权限

```typescript
export async function hasFullPermission(userId: string): Promise<boolean> {
  const { data } = await supabase
    .from('profiles')
    .select('role, permission_level')
    .eq('id', userId)
    .single()
  
  if (!data) return false
  
  // 老板始终拥有完整权限
  if (data.role === 'boss') return true
  
  // 平级账号和车队长需要检查权限级别
  if (data.role === 'peer' || data.role === 'manager') {
    return data.permission_level === 'full_permission'
  }
  
  return false
}
```

### 检查管辖范围

```typescript
export async function canManageWarehouse(
  userId: string,
  warehouseId: string
): Promise<boolean> {
  const { data } = await supabase
    .from('profiles')
    .select('role, permission_level, managed_warehouses')
    .eq('id', userId)
    .single()
  
  if (!data) return false
  
  // 老板和拥有完整权限的平级账号可以管理所有仓库
  if (data.role === 'boss') return true
  if (data.role === 'peer' && data.permission_level === 'full_permission') return true
  
  // 车队长只能管理分配给自己的仓库
  if (data.role === 'manager' && data.permission_level === 'full_permission') {
    return data.managed_warehouses?.includes(warehouseId) ?? false
  }
  
  return false
}
```

---

## 📝 使用示例

### 示例 1：老板创建平级账号

```typescript
import { createPeerAccount } from '@/db/accountApi'

// 老板登录后
const peer = await createPeerAccount({
  boss_id: currentUser.id,
  real_name: '李四',
  phone: '13800000002',
  email: 'lisi@example.com',
  password: 'password123',
  permission_level: 'full_permission'
})

console.log('平级账号创建成功:', peer)
```

### 示例 2：车队长查看管辖范围内的司机

```typescript
import { getDriversByManager } from '@/db/driverApi'

// 车队长登录后
const drivers = await getDriversByManager(currentUser.id)

console.log('管辖范围内的司机:', drivers)
```

### 示例 3：检查用户权限

```typescript
import { hasFullPermission, canManageWarehouse } from '@/db/permissionApi'

// 检查是否拥有完整权限
const hasFull = await hasFullPermission(currentUser.id)

if (hasFull) {
  console.log('用户拥有完整权限')
} else {
  console.log('用户只有只读权限')
}

// 检查是否可以管理指定仓库
const canManage = await canManageWarehouse(currentUser.id, warehouseId)

if (canManage) {
  console.log('可以管理该仓库')
} else {
  console.log('无权管理该仓库')
}
```

---

## 🎯 总结

### 权限层级

```
超级管理员（中央管理系统）
  └── 管理所有租户
  
老板（租户系统最高权限）
  ├── 平级账号（最多3个）
  │   ├── 完整权限：与老板相同
  │   └── 只读权限：只能查看
  ├── 车队长
  │   ├── 完整权限：管辖范围内最高权限
  │   └── 只读权限：管辖范围内只能查看
  └── 司机
      └── 只能操作自己的数据
```

### 关键点

1. **超级管理员**：只管理租户配置，不直接操作业务数据
2. **老板**：租户内的最高权限所有者
3. **平级账号**：协助老板管理，权限由老板授予
4. **车队长**：管理指定范围，权限由老板或平级账号（完整权限）授予
5. **司机**：基层操作人员，只能操作自己的数据

---

**文档版本**：v1.0  
**更新日期**：2025-11-05  
**维护者**：开发团队

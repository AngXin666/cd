# 用户迁移脚本使用说明

## 📋 概述

本目录包含用于将所有租户下的车队长和司机迁移到指定租户的脚本。

## 🔍 当前数据库状态

**重要提示**：经过检查，当前数据库中**没有任何用户数据**。

这意味着：
- 没有租户（super_admin）
- 没有车队长（manager）
- 没有司机（driver）

因此，**目前无法执行迁移操作**。

## 📂 脚本文件说明

### 1. 查询脚本

| 脚本文件 | 功能 | 用途 |
|---------|------|------|
| `list-all-users.ts` | 列出所有用户 | 查看数据库中的所有用户及其角色 |
| `list-tenants.ts` | 列出所有租户 | 查看所有主账号（租户） |
| `list-all-super-admins.ts` | 列出所有 super_admin | 查看所有老板账号 |

### 2. 迁移脚本

| 脚本文件 | 功能 | 用途 |
|---------|------|------|
| `migrate-users.ts` | 用户迁移脚本 | 将所有租户下的车队长和司机迁移到目标租户 |
| `migrate-users-to-target-tenant.sql` | SQL 迁移脚本 | 直接在数据库中执行迁移（备用方案） |

## 🚀 使用步骤

### 步骤 1：查看当前数据库状态

```bash
# 查看所有用户
cd /workspace/app-7cdqf07mbu9t
npx tsx scripts/list-all-users.ts

# 查看所有租户
npx tsx scripts/list-tenants.ts
```

### 步骤 2：确认目标租户存在

在执行迁移之前，请确保：
1. 手机号为 `13800000001` 的租户（主账号）已存在
2. 该租户的 `role` 为 `super_admin`
3. 该租户的 `main_account_id` 为 `NULL`（表示是主账号）

### 步骤 3：执行迁移

```bash
# 使用 TypeScript 脚本迁移
cd /workspace/app-7cdqf07mbu9t
npx tsx scripts/migrate-users.ts
```

## 📊 迁移脚本功能

### 迁移规则

1. **迁移对象**：
   - ✅ 车队长（role = 'manager'）
   - ✅ 司机（role = 'driver'）
   - ❌ 老板账号（role = 'super_admin'）- 不迁移

2. **迁移条件**：
   - 只迁移 `boss_id` 不等于目标租户 ID 的用户
   - 或者 `boss_id` 为 NULL 的用户

3. **迁移操作**：
   - 更新用户的 `boss_id` 为目标租户 ID
   - 更新 `updated_at` 时间戳

### 迁移流程

```
1. 查询目标租户（手机号：13800000001）
   ↓
2. 统计需要迁移的用户
   - 车队长数量
   - 司机数量
   ↓
3. 执行迁移
   - 更新车队长的 boss_id
   - 更新司机的 boss_id
   ↓
4. 验证迁移结果
   - 统计目标租户下的用户数量
   - 显示迁移报告
```

## 📝 迁移示例

### 迁移前

```
租户 A (ID: xxx-aaa)
├── 车队长 1
├── 车队长 2
└── 司机 1-10

租户 B (ID: xxx-bbb)
├── 车队长 3
└── 司机 11-20

目标租户 (手机号: 13800000001, ID: xxx-target)
└── (空)
```

### 迁移后

```
租户 A (ID: xxx-aaa)
└── (空 - 车队长和司机已迁移)

租户 B (ID: xxx-bbb)
└── (空 - 车队长和司机已迁移)

目标租户 (手机号: 13800000001, ID: xxx-target)
├── 车队长 1
├── 车队长 2
├── 车队长 3
├── 司机 1-10
└── 司机 11-20
```

## ⚠️ 注意事项

### 1. 数据备份

在执行迁移之前，建议先备份数据库：

```bash
# 使用 Supabase Dashboard 导出数据
# 或使用 pg_dump 命令备份
```

### 2. 测试环境

建议先在测试环境中执行迁移，验证无误后再在生产环境执行。

### 3. 权限检查

确保执行脚本的账号有足够的权限：
- 读取 profiles 表
- 更新 profiles 表

### 4. 关联数据

迁移用户时，以下关联数据**不会自动迁移**，需要单独处理：
- 用户的考勤记录（attendance_records）
- 用户的请假记录（leave_requests）
- 用户的工资记录（salaries）
- 用户的仓库关联（user_warehouses）
- 其他业务数据

如果需要迁移这些关联数据，请联系开发人员创建专门的迁移脚本。

## 🔧 故障排除

### 问题 1：找不到目标租户

**错误信息**：
```
❌ 未找到手机号为 13800000001 的租户（主账号）
```

**解决方案**：
1. 检查手机号是否正确
2. 确认该用户已注册并且角色为 `super_admin`
3. 确认该用户的 `main_account_id` 为 `NULL`（主账号）

### 问题 2：没有需要迁移的用户

**提示信息**：
```
✅ 没有需要迁移的用户
```

**说明**：
- 所有车队长和司机已经在目标租户下
- 或者数据库中没有车队长和司机

### 问题 3：迁移失败

**错误信息**：
```
❌ 迁移车队长 XXX 失败
```

**解决方案**：
1. 检查数据库连接
2. 检查用户权限
3. 查看详细错误信息
4. 联系开发人员

## 📞 技术支持

如果在使用过程中遇到问题，请：
1. 查看脚本输出的详细日志
2. 检查数据库连接配置（.env 文件）
3. 联系开发人员获取帮助

## 📚 相关文档

- [用户管理系统架构](../docs/USER_MANAGEMENT_ARCHITECTURE.md)
- [租户管理系统](../docs/TENANT_MANAGEMENT.md)
- [数据库设计文档](../supabase/migrations/README.md)

---

**创建日期**：2025-11-05  
**最后更新**：2025-11-05  
**维护人员**：秒哒 AI

# Fleet Manager Backend Dependencies

# FastAPI core framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Database ORM
sqlmodel>=0.0.14

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Environment variables
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# File upload
python-multipart>=0.0.6

# OCR - 支持两种方式
# 1. 百度 OCR API（需要配置 API Key）
# 2. PaddleOCR 本地识别（免费，无需注册）
# 安装 PaddleOCR: pip install paddlepaddle paddleocr
# 注意：PaddleOCR 首次运行会下载模型（约 100MB）

# Database drivers
psycopg2-binary>=2.9.0

# HTTP client (用于 OCR API 调用等)
httpx>=0.24.0

# Scheduler for scheduled notifications
apscheduler>=3.10.0

# Data migration (from Supabase) - 仅迁移脚本使用
# 如果迁移已完成，可以移除此依赖
supabase>=2.0.0

# Testing
pytest>=7.0.0
hypothesis>=6.0.0

# Fleet Manager Backend Dependencies

# FastAPI core framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Database ORM
sqlmodel>=0.0.14

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Environment variables
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# File upload
python-multipart>=0.0.6

# OCR - 使用 HTTP API 直接调用百度 OCR，不需要 baidu-aip SDK
# baidu-aip>=4.16.0  # 已移除：未使用，OCR 通过 httpx 直接调用 API

# Database drivers
psycopg2-binary>=2.9.0

# HTTP client (用于 OCR API 调用等)
httpx>=0.24.0

# Scheduler for scheduled notifications
apscheduler>=3.10.0

# Data migration (from Supabase) - 仅迁移脚本使用
# 如果迁移已完成，可以移除此依赖
supabase>=2.0.0

# Testing
pytest>=7.0.0
hypothesis>=6.0.0

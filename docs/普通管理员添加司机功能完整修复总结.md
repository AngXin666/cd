# 普通管理员添加司机功能完整修复总结

## 问题概述

普通管理员在添加司机时遇到多个错误，导致功能无法正常使用。本文档总结了所有问题和修复方案。

## 问题列表

### 问题 1：邮箱重复错误

**错误信息**：
```
duplicate key value violates unique constraint "users_email_partial_key"
```

**原因**：
- 创建用户时，auth.users 创建成功但 profiles 创建失败
- 导致孤立的 auth.users 记录
- 再次尝试创建时，邮箱重复

**解决方案**：
1. 更新 `create_user_auth_account_first` 函数，自动检测并清理孤立记录
2. 创建 `cleanup_orphaned_auth_users` 函数，支持手动清理
3. 移除对 auth.users 的直接查询，避免权限错误

**相关文件**：
- `supabase/migrations/017_handle_duplicate_auth_users.sql`
- `docs/修复邮箱重复错误.md`

### 问题 2：RLS 策略错误

**错误信息**：
```
new row violates row-level security policy for table "profiles"
错误代码: 42501
```

**原因**：
- `is_manager` 函数的 `search_path` 只包含 `public`
- 无法找到 `auth` schema 中的函数
- 导致策略检查失败

**解决方案**：
1. 更新 `is_manager` 函数，添加 `auth` schema 到 `search_path`
2. 更新 `is_super_admin` 函数，设置正确的 `search_path`

**相关文件**：
- `supabase/migrations/018_fix_manager_insert_policy.sql`
- `supabase/migrations/019_fix_is_manager_search_path.sql`
- `supabase/migrations/020_fix_is_super_admin_search_path.sql`
- `docs/修复RLS策略错误.md`

### 问题 3：创建顺序错误

**错误信息**：
```
insert or update on table "profiles" violates foreign key constraint "profiles_id_fkey"
```

**原因**：
- 先创建 profiles 记录，再创建 auth.users 记录
- profiles.id 引用 auth.users.id（外键约束）
- 创建时 auth.users 记录不存在，导致外键约束失败

**解决方案**：
1. 修改 `createDriver` 函数，先创建 auth.users，再创建 profiles
2. 使用 `create_user_auth_account_first` 函数创建 auth.users
3. 使用返回的 user_id 创建 profiles 记录

**相关文件**：
- `src/db/api.ts` - createDriver 函数
- `docs/修复记录_普通管理员添加司机.md`

### 问题 4：权限不足

**错误信息**：
```
relation "public.auth.users" does not exist
```

**原因**：
- 普通用户没有权限直接查询 auth.users 表
- 代码中尝试直接查询 auth.users 检查邮箱重复

**解决方案**：
1. 移除对 auth.users 的直接查询
2. 只检查 profiles 表
3. 依赖 `create_user_auth_account_first` 函数处理重复情况

**相关文件**：
- `src/db/api.ts` - createDriver 和 createUser 函数

## 完整修复流程

### 1. 数据库层面

#### 1.1 创建用户认证函数

```sql
-- 创建 auth.users 记录的函数
CREATE OR REPLACE FUNCTION create_user_auth_account_first(
  user_email text,
  user_phone text
)
RETURNS jsonb
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, auth, extensions
AS $$
DECLARE
  new_user_id uuid;
  hashed_password text;
  existing_user_id uuid;
  has_profile boolean;
BEGIN
  -- 检查邮箱是否已存在
  SELECT id INTO existing_user_id
  FROM auth.users
  WHERE email = user_email OR phone = user_phone;

  IF existing_user_id IS NOT NULL THEN
    -- 检查是否有对应的 profiles 记录
    SELECT EXISTS(
      SELECT 1 FROM profiles WHERE id = existing_user_id
    ) INTO has_profile;

    IF has_profile THEN
      -- 用户已完整创建，返回错误
      RETURN jsonb_build_object(
        'success', false,
        'error', 'duplicate_user',
        'details', '用户已存在'
      );
    ELSE
      -- 孤立记录，删除它
      DELETE FROM auth.users WHERE id = existing_user_id;
    END IF;
  END IF;

  -- 生成密码哈希（默认密码：123456）
  hashed_password := crypt('123456', gen_salt('bf'));

  -- 创建新用户
  INSERT INTO auth.users (
    instance_id, id, aud, role, email, encrypted_password,
    email_confirmed_at, phone, phone_confirmed_at,
    raw_app_meta_data, raw_user_meta_data,
    created_at, updated_at,
    confirmation_token, email_change, email_change_token_new, recovery_token
  ) VALUES (
    '00000000-0000-0000-0000-000000000000',
    gen_random_uuid(),
    'authenticated', 'authenticated',
    user_email, hashed_password,
    now(), user_phone, now(),
    '{"provider":"email","providers":["email"]}', '{}',
    now(), now(),
    '', '', '', ''
  )
  RETURNING id INTO new_user_id;

  -- 返回成功结果
  RETURN jsonb_build_object(
    'success', true,
    'user_id', new_user_id,
    'email', user_email,
    'phone', user_phone,
    'default_password', '123456'
  );
EXCEPTION
  WHEN OTHERS THEN
    RETURN jsonb_build_object(
      'success', false,
      'error', SQLERRM,
      'details', SQLSTATE
    );
END;
$$;
```

#### 1.2 创建清理函数

```sql
-- 清理孤立 auth.users 记录的函数
CREATE OR REPLACE FUNCTION cleanup_orphaned_auth_users()
RETURNS jsonb
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, auth
AS $$
DECLARE
  deleted_count int;
BEGIN
  -- 删除没有对应 profiles 记录的 auth.users
  WITH orphaned_users AS (
    SELECT au.id
    FROM auth.users au
    LEFT JOIN profiles p ON au.id = p.id
    WHERE p.id IS NULL
  )
  DELETE FROM auth.users
  WHERE id IN (SELECT id FROM orphaned_users);

  GET DIAGNOSTICS deleted_count = ROW_COUNT;

  RETURN jsonb_build_object(
    'success', true,
    'deleted_count', deleted_count,
    'message', format('已清理 %s 条孤立的 auth.users 记录', deleted_count)
  );
EXCEPTION
  WHEN OTHERS THEN
    RETURN jsonb_build_object(
      'success', false,
      'error', SQLERRM,
      'details', SQLSTATE
    );
END;
$$;
```

#### 1.3 更新权限检查函数

```sql
-- 更新 is_manager 函数
CREATE OR REPLACE FUNCTION is_manager(uid uuid)
RETURNS boolean
LANGUAGE sql
SECURITY DEFINER
SET search_path TO 'public', 'auth'
AS $$
  SELECT EXISTS (
    SELECT 1 FROM profiles p
    WHERE p.id = uid AND p.role = 'manager'::user_role
  );
$$;

-- 更新 is_super_admin 函数
CREATE OR REPLACE FUNCTION is_super_admin(uid uuid)
RETURNS boolean
LANGUAGE sql
SECURITY DEFINER
SET search_path TO 'public', 'auth'
AS $$
  SELECT EXISTS (
    SELECT 1 FROM profiles
    WHERE id = uid AND role = 'super_admin'::user_role
  );
$$;
```

#### 1.4 创建 RLS 策略

```sql
-- 允许普通管理员插入司机记录
CREATE POLICY "Managers can insert driver profiles" ON profiles
  FOR INSERT TO authenticated
  WITH CHECK (
    is_manager(auth.uid()) AND role = 'driver'::user_role
  );
```

### 2. 应用层面

#### 2.1 更新 createDriver 函数

```typescript
export async function createDriver(
  phone: string,
  name: string,
  driverType: string
): Promise<Profile | null> {
  const timestamp = new Date().toISOString()
  console.log(`\n${'='.repeat(80)}`)
  console.log('🚀 [createDriver] 开始创建司机')
  console.log('⏰ 时间戳:', timestamp)
  console.log('📱 输入参数:')
  console.log('  - 手机号:', phone)
  console.log('  - 姓名:', name)
  console.log('  - 司机类型:', driverType)
  console.log(`${'='.repeat(80)}\n`)

  try {
    // 步骤1: 检查手机号是否已存在
    console.log('📋 [步骤1] 检查手机号是否已存在')
    const {data: existingProfiles, error: checkError} = await supabase
      .from('profiles')
      .select('*')
      .eq('phone', phone)
      .maybeSingle()

    if (checkError) {
      console.error('  ❌ 查询 profiles 失败:', checkError)
      return null
    }

    if (existingProfiles) {
      console.warn('  ⚠️ 手机号已存在')
      return null
    }

    console.log('  ✅ 手机号可用\n')

    // 步骤2: 先创建 auth.users 表记录
    console.log('📋 [步骤2] 创建 auth.users 表记录')
    const loginEmail = `${phone}@fleet.com`
    
    const {data: authResult, error: authError} = await supabase.rpc(
      'create_user_auth_account_first',
      {
        user_email: loginEmail,
        user_phone: phone
      }
    )

    if (authError || !authResult || authResult.success === false) {
      console.error('  ❌ 创建 auth.users 记录失败')
      return null
    }

    const userId = authResult.user_id
    console.log('  ✅ auth.users 记录创建成功')
    console.log('  - 用户ID:', userId)
    console.log('  - 登录邮箱:', loginEmail)
    console.log('  - 默认密码: 123456\n')

    // 步骤3: 创建 profiles 表记录
    console.log('📋 [步骤3] 创建 profiles 表记录')
    const profileData = {
      id: userId,
      phone: phone,
      name: name,
      role: 'driver' as const,
      driver_type: driverType
    }

    const {data: profile, error: profileError} = await supabase
      .from('profiles')
      .insert(profileData)
      .select()
      .maybeSingle()

    if (profileError) {
      console.error('  ❌ 插入失败:', profileError)
      return null
    }

    console.log('  ✅ profiles 记录创建成功')
    console.log('  - 用户ID:', profile.id)
    console.log('  - 姓名:', profile.name)
    console.log('  - 角色:', profile.role)
    console.log('  - 司机类型:', profile.driver_type)
    console.log(`\n${'='.repeat(80)}`)
    console.log('✅ [createDriver] 司机创建完成')
    console.log(`${'='.repeat(80)}\n`)

    return profile
  } catch (error) {
    console.error('❌ [createDriver] 创建司机时发生异常:', error)
    return null
  }
}
```

## 测试验证

### 测试 1：正常创建司机

**步骤**：
1. 使用普通管理员账号登录
2. 进入司机管理页面
3. 添加新司机（使用未使用过的手机号）
4. 点击"添加"按钮

**预期结果**：
- ✅ 创建成功
- ✅ 显示登录信息弹窗
- ✅ auth.users 和 profiles 都有记录
- ✅ ID 一致

### 测试 2：处理孤立记录

**步骤**：
1. 手动创建一个孤立的 auth.users 记录
2. 使用相同手机号添加司机

**预期结果**：
- ✅ 自动清理孤立记录
- ✅ 创建新的完整记录
- ✅ 显示登录信息弹窗

### 测试 3：重复手机号检测

**步骤**：
1. 创建一个完整的司机记录
2. 尝试使用相同手机号再次创建

**预期结果**：
- ✅ 显示错误提示
- ✅ 不创建新记录

### 测试 4：权限边界

**测试 4.1**：普通管理员不能创建管理员
- ❌ 创建失败
- ✅ RLS 策略阻止

**测试 4.2**：普通管理员不能创建超级管理员
- ❌ 创建失败
- ✅ RLS 策略阻止

**测试 4.3**：司机不能创建任何用户
- ❌ 无法访问页面或创建失败

## 关键技术点

### 1. 创建顺序

**正确顺序**：
1. 先创建 auth.users（认证记录）
2. 再创建 profiles（业务记录）
3. 使用相同的 ID

**原因**：
- profiles.id 引用 auth.users.id（外键约束）
- 必须先有父记录，才能创建子记录

### 2. 权限设计

**SECURITY DEFINER**：
- 函数以定义者权限执行
- 允许普通用户执行需要更高权限的操作
- 类似于 Linux 的 setuid

**search_path**：
- 指定查找对象的 schema 顺序
- 必须包含 auth schema 才能使用 auth.uid()

**RLS 策略**：
- 双重检查：调用者身份 + 插入数据
- 确保普通管理员只能创建司机

### 3. 错误处理

**孤立记录处理**：
- 自动检测：在创建前检查
- 自动清理：删除孤立记录
- 手动清理：提供清理函数

**权限错误处理**：
- 移除直接查询：避免权限错误
- 使用函数封装：通过 SECURITY DEFINER 提权
- 依赖策略检查：让数据库处理权限

## 维护建议

### 1. 定期清理

```sql
-- 每周执行一次
SELECT * FROM cleanup_orphaned_auth_users();
```

### 2. 监控孤立记录

```sql
-- 监控查询
SELECT 
  COUNT(*) as orphaned_count,
  MIN(created_at) as oldest_orphaned,
  MAX(created_at) as newest_orphaned
FROM auth.users au
LEFT JOIN profiles p ON au.id = p.id
WHERE p.id IS NULL;
```

### 3. 审计日志

```sql
-- 记录清理操作
CREATE TABLE IF NOT EXISTS cleanup_logs (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  operation text NOT NULL,
  deleted_count int NOT NULL,
  executed_at timestamptz DEFAULT now(),
  executed_by uuid REFERENCES profiles(id)
);
```

## 相关文档

- `docs/修复邮箱重复错误.md` - 邮箱重复错误的详细说明
- `docs/修复RLS策略错误.md` - RLS 策略错误的详细说明
- `docs/修复记录_普通管理员添加司机.md` - 修复过程记录
- `docs/普通管理员添加司机功能测试.md` - 测试文档

## 总结

通过以下措施，完全修复了普通管理员添加司机功能：

1. ✅ 修复创建顺序：先创建 auth.users，再创建 profiles
2. ✅ 处理孤立记录：自动检测和清理
3. ✅ 修复权限错误：移除直接查询，使用函数封装
4. ✅ 修复 RLS 策略：更新 search_path
5. ✅ 完善错误处理：提供详细的日志和错误信息
6. ✅ 确保安全性：权限边界清晰，策略严格

现在，普通管理员可以成功添加司机，系统运行稳定，安全性得到保障。

# 超级管理员车辆审核页面显示司机完整实名信息功能报告

## 实现时间
2025-11-22

## 需求描述
超级管理员在车辆管理功能中对应车辆的查看司机信息页面应该直接读取司机的实名信息资料，包括：
- 姓名、身份证号码、住址、出生日期、年龄
- 对应公安局（签发机关）
- 身份证照片（正反面）
- 驾驶证信息：驾驶证类型（准驾车型）、初次领证日期、驾龄
- 驾驶证照片

---

## 实现内容

### 1. 数据类型定义

#### 新增类型：VehicleWithDriverDetails

**文件**：`src/db/types.ts`

```typescript
// 车辆信息（包含司机详细信息）
export interface VehicleWithDriverDetails extends Vehicle {
  driver_profile?: {
    name: string | null
    phone: string | null
    email: string | null
  } | null
  driver_license?: {
    // 身份证信息
    id_card_name: string | null
    id_card_number: string | null
    id_card_address: string | null
    id_card_birth_date: string | null
    id_card_photo_front: string | null
    id_card_photo_back: string | null
    // 驾驶证信息
    license_number: string | null
    license_class: string | null
    first_issue_date: string | null
    valid_from: string | null
    valid_to: string | null
    issue_authority: string | null
    driving_license_photo: string | null
  } | null
}
```

**说明**：
- `driver_profile`：司机基本信息，来自`profiles`表
- `driver_license`：司机证件信息，来自`driver_licenses`表
- 包含身份证和驾驶证的完整信息

### 2. 数据库API函数

#### 新增函数：getVehicleWithDriverDetails

**文件**：`src/db/api.ts`

```typescript
/**
 * 根据车辆ID获取车辆信息（包含司机详细信息）
 */
export async function getVehicleWithDriverDetails(vehicleId: string): Promise<VehicleWithDriverDetails | null> {
  logger.db('查询', 'vehicles with driver details', {vehicleId})
  try {
    // 1. 获取车辆基本信息
    const vehicle = await getVehicleById(vehicleId)
    if (!vehicle) {
      logger.warn('车辆不存在', {vehicleId})
      return null
    }

    // 2. 获取司机基本信息（从profiles表）
    const {data: profile, error: profileError} = await supabase
      .from('profiles')
      .select('name, phone, email')
      .eq('id', vehicle.user_id)
      .maybeSingle()

    if (profileError) {
      logger.error('获取司机基本信息失败', {error: profileError})
    }

    // 3. 获取司机证件信息（从driver_licenses表）
    const {data: driverLicense, error: licenseError} = await supabase
      .from('driver_licenses')
      .select(
        'id_card_name, id_card_number, id_card_address, id_card_birth_date, id_card_photo_front, id_card_photo_back, license_number, license_class, first_issue_date, valid_from, valid_to, issue_authority, driving_license_photo'
      )
      .eq('driver_id', vehicle.user_id)
      .maybeSingle()

    if (licenseError) {
      logger.error('获取司机证件信息失败', {error: licenseError})
    }

    // 4. 组合数据
    const result: VehicleWithDriverDetails = {
      ...vehicle,
      driver_profile: profile ? {...} : null,
      driver_license: driverLicense ? {...} : null
    }

    logger.info('成功获取车辆和司机详细信息', {vehicleId, hasProfile: !!profile, hasLicense: !!driverLicense})
    return result
  } catch (error) {
    logger.error('获取车辆和司机详细信息异常', error)
    return null
  }
}
```

**功能说明**：
1. 先获取车辆基本信息
2. 根据车辆的`user_id`查询司机基本信息（profiles表）
3. 根据`user_id`查询司机证件信息（driver_licenses表）
4. 将所有信息组合返回

### 3. 辅助计算函数

#### 计算年龄函数

**文件**：`src/pages/super-admin/vehicle-review-detail/index.tsx`

```typescript
// 计算年龄
const calculateAge = (birthDate: string | null): number | null => {
  if (!birthDate) return null
  const birth = new Date(birthDate)
  const today = new Date()
  let age = today.getFullYear() - birth.getFullYear()
  const monthDiff = today.getMonth() - birth.getMonth()
  if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < birth.getDate())) {
    age--
  }
  return age
}
```

**功能**：根据出生日期自动计算当前年龄

#### 计算驾龄函数

```typescript
// 计算驾龄
const calculateDrivingYears = (firstIssueDate: string | null): number | null => {
  if (!firstIssueDate) return null
  const issueDate = new Date(firstIssueDate)
  const today = new Date()
  let years = today.getFullYear() - issueDate.getFullYear()
  const monthDiff = today.getMonth() - issueDate.getMonth()
  if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < issueDate.getDate())) {
    years--
  }
  return years
}
```

**功能**：根据初次领证日期自动计算驾龄

### 4. 页面UI实现

#### 4.1 基本信息卡片

```tsx
{/* 基本信息 */}
{vehicle.driver_profile && (
  <View className="mb-4">
    <View className="flex items-center mb-2">
      <View className="i-mdi-account text-blue-600 text-lg mr-2"></View>
      <Text className="text-base font-bold text-gray-700">基本信息</Text>
    </View>
    {vehicle.driver_profile.name && (
      <View className="flex items-start mb-1.5 ml-7">
        <Text className="text-sm text-gray-600 w-20">姓名：</Text>
        <Text className="text-sm text-gray-900 font-medium flex-1">
          {vehicle.driver_profile.name}
        </Text>
      </View>
    )}
    {vehicle.driver_profile.phone && (
      <View className="flex items-start mb-1.5 ml-7">
        <Text className="text-sm text-gray-600 w-20">电话：</Text>
        <Text className="text-sm text-gray-900 flex-1">{vehicle.driver_profile.phone}</Text>
      </View>
    )}
    {vehicle.driver_profile.email && (
      <View className="flex items-start ml-7">
        <Text className="text-sm text-gray-600 w-20">邮箱：</Text>
        <Text className="text-sm text-gray-900 flex-1">{vehicle.driver_profile.email}</Text>
      </View>
    )}
  </View>
)}
```

**显示内容**：
- ✅ 姓名
- ✅ 电话
- ✅ 邮箱

#### 4.2 身份证信息卡片（蓝色渐变背景）

```tsx
{/* 身份证信息 */}
<View className="mb-4 bg-gradient-to-r from-blue-50 to-indigo-50 rounded-xl p-3 border border-blue-100">
  <View className="flex items-center mb-2">
    <View className="i-mdi-card-account-details text-blue-600 text-lg mr-2"></View>
    <Text className="text-base font-bold text-blue-800">身份证信息</Text>
  </View>
  {/* 姓名 */}
  {vehicle.driver_license.id_card_name && (
    <View className="flex items-start mb-1.5 ml-7">
      <Text className="text-sm text-gray-600 w-20">姓名：</Text>
      <Text className="text-sm text-gray-900 font-medium flex-1">
        {vehicle.driver_license.id_card_name}
      </Text>
    </View>
  )}
  {/* 身份证号 */}
  {vehicle.driver_license.id_card_number && (
    <View className="flex items-start mb-1.5 ml-7">
      <Text className="text-sm text-gray-600 w-20">身份证号：</Text>
      <Text className="text-sm text-gray-900 flex-1">
        {vehicle.driver_license.id_card_number}
      </Text>
    </View>
  )}
  {/* 出生日期和年龄 */}
  {vehicle.driver_license.id_card_birth_date && (
    <View className="flex items-start mb-1.5 ml-7">
      <Text className="text-sm text-gray-600 w-20">出生日期：</Text>
      <Text className="text-sm text-gray-900 flex-1">
        {vehicle.driver_license.id_card_birth_date}
        {calculateAge(vehicle.driver_license.id_card_birth_date) !== null && (
          <Text className="text-blue-600 font-medium ml-2">
            ({calculateAge(vehicle.driver_license.id_card_birth_date)}岁)
          </Text>
        )}
      </Text>
    </View>
  )}
  {/* 住址 */}
  {vehicle.driver_license.id_card_address && (
    <View className="flex items-start mb-1.5 ml-7">
      <Text className="text-sm text-gray-600 w-20">住址：</Text>
      <Text className="text-sm text-gray-900 flex-1">
        {vehicle.driver_license.id_card_address}
      </Text>
    </View>
  )}
  {/* 签发机关 */}
  {vehicle.driver_license.issue_authority && (
    <View className="flex items-start ml-7">
      <Text className="text-sm text-gray-600 w-20">签发机关：</Text>
      <Text className="text-sm text-gray-900 flex-1">
        {vehicle.driver_license.issue_authority}
      </Text>
    </View>
  )}
</View>
```

**显示内容**：
- ✅ 姓名（实名）
- ✅ 身份证号码
- ✅ 出生日期 + 自动计算的年龄
- ✅ 住址
- ✅ 签发机关（对应公安局）

#### 4.3 驾驶证信息卡片（绿色渐变背景）

```tsx
{/* 驾驶证信息 */}
<View className="mb-4 bg-gradient-to-r from-green-50 to-emerald-50 rounded-xl p-3 border border-green-100">
  <View className="flex items-center mb-2">
    <View className="i-mdi-card-account-details text-green-600 text-lg mr-2"></View>
    <Text className="text-base font-bold text-green-800">驾驶证信息</Text>
  </View>
  {/* 驾驶证号 */}
  {vehicle.driver_license.license_number && (
    <View className="flex items-start mb-1.5 ml-7">
      <Text className="text-sm text-gray-600 w-24">驾驶证号：</Text>
      <Text className="text-sm text-gray-900 flex-1">
        {vehicle.driver_license.license_number}
      </Text>
    </View>
  )}
  {/* 准驾车型 */}
  {vehicle.driver_license.license_class && (
    <View className="flex items-start mb-1.5 ml-7">
      <Text className="text-sm text-gray-600 w-24">准驾车型：</Text>
      <Text className="text-sm text-gray-900 font-medium flex-1">
        {vehicle.driver_license.license_class}
      </Text>
    </View>
  )}
  {/* 初次领证日期和驾龄 */}
  {vehicle.driver_license.first_issue_date && (
    <View className="flex items-start mb-1.5 ml-7">
      <Text className="text-sm text-gray-600 w-24">初次领证日期：</Text>
      <Text className="text-sm text-gray-900 flex-1">
        {vehicle.driver_license.first_issue_date}
        {calculateDrivingYears(vehicle.driver_license.first_issue_date) !== null && (
          <Text className="text-green-600 font-medium ml-2">
            (驾龄{calculateDrivingYears(vehicle.driver_license.first_issue_date)}年)
          </Text>
        )}
      </Text>
    </View>
  )}
  {/* 有效期 */}
  {vehicle.driver_license.valid_from && vehicle.driver_license.valid_to && (
    <View className="flex items-start ml-7">
      <Text className="text-sm text-gray-600 w-24">有效期：</Text>
      <Text className="text-sm text-gray-900 flex-1">
        {vehicle.driver_license.valid_from} 至 {vehicle.driver_license.valid_to}
      </Text>
    </View>
  )}
</View>
```

**显示内容**：
- ✅ 驾驶证号
- ✅ 准驾车型（驾驶证类型）
- ✅ 初次领证日期 + 自动计算的驾龄
- ✅ 有效期

#### 4.4 证件照片展示

```tsx
{/* 证件照片 */}
<View className="bg-gray-50 rounded-xl p-3">
  <View className="flex items-center mb-2">
    <View className="i-mdi-image-multiple text-gray-600 text-lg mr-2"></View>
    <Text className="text-base font-bold text-gray-700">证件照片</Text>
  </View>
  <View className="grid grid-cols-2 gap-3">
    {/* 身份证正面 */}
    {vehicle.driver_license.id_card_photo_front && (
      <View onClick={() => previewImage(...)}>
        <Text className="text-xs text-gray-500 mb-1 block">身份证正面</Text>
        <Image
          src={getImageUrl(vehicle.driver_license.id_card_photo_front)}
          mode="aspectFill"
          className="w-full h-32 rounded-lg"
        />
      </View>
    )}
    {/* 身份证背面 */}
    {vehicle.driver_license.id_card_photo_back && (
      <View onClick={() => previewImage(...)}>
        <Text className="text-xs text-gray-500 mb-1 block">身份证背面</Text>
        <Image
          src={getImageUrl(vehicle.driver_license.id_card_photo_back)}
          mode="aspectFill"
          className="w-full h-32 rounded-lg"
        />
      </View>
    )}
    {/* 驾驶证照片 */}
    {vehicle.driver_license.driving_license_photo && (
      <View onClick={() => previewImage(...)}>
        <Text className="text-xs text-gray-500 mb-1 block">驾驶证照片</Text>
        <Image
          src={getImageUrl(vehicle.driver_license.driving_license_photo)}
          mode="aspectFill"
          className="w-full h-32 rounded-lg"
        />
      </View>
    )}
  </View>
</View>
```

**显示内容**：
- ✅ 身份证正面照片
- ✅ 身份证背面照片
- ✅ 驾驶证照片
- ✅ 所有照片支持点击放大预览

---

## 功能展示

### 页面布局

```
┌─────────────────────────────────────────┐
│ 🚗 车辆信息                              │
│ 京A12345                                 │
│ 奔驰 E300                                │
│ 颜色：黑色                               │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 👤 司机信息                              │
│                                          │
│ 📋 基本信息                              │
│    姓名：      张三                      │
│    电话：      13800138000               │
│    邮箱：      zhangsan@example.com      │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ 🆔 身份证信息（蓝色渐变背景）        │ │
│ │    姓名：      张三                  │ │
│ │    身份证号：  110101199001011234    │ │
│ │    出生日期：  1990-01-01 (34岁)    │ │
│ │    住址：      北京市朝阳区...       │ │
│ │    签发机关：  北京市公安局朝阳分局  │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ 🚗 驾驶证信息（绿色渐变背景）        │ │
│ │    驾驶证号：  110101199001011234    │ │
│ │    准驾车型：  C1                    │ │
│ │    初次领证日期：2010-05-20 (驾龄14年)│ │
│ │    有效期：    2020-05-20 至 2026-05-20│ │
│ └─────────────────────────────────────┘ │
│                                          │
│ 📷 证件照片                              │
│ ┌──────────┐  ┌──────────┐             │
│ │身份证正面│  │身份证背面│             │
│ │  [照片]  │  │  [照片]  │             │
│ └──────────┘  └──────────┘             │
│ ┌──────────┐                            │
│ │驾驶证照片│                            │
│ │  [照片]  │                            │
│ └──────────┘                            │
└─────────────────────────────────────────┘
```

---

## 数据来源

### 数据表关系

```
vehicles (车辆表)
    └─ user_id → profiles (司机基本信息表)
                 ├─ name (姓名)
                 ├─ phone (电话)
                 └─ email (邮箱)
                 
    └─ user_id → driver_licenses (司机证件信息表)
                 ├─ id_card_name (身份证姓名) ✨
                 ├─ id_card_number (身份证号) ✨
                 ├─ id_card_address (住址) ✨
                 ├─ id_card_birth_date (出生日期) ✨
                 ├─ id_card_photo_front (身份证正面) ✨
                 ├─ id_card_photo_back (身份证背面) ✨
                 ├─ issue_authority (签发机关) ✨
                 ├─ license_number (驾驶证号) ✨
                 ├─ license_class (准驾车型) ✨
                 ├─ first_issue_date (初次领证日期) ✨
                 ├─ valid_from (有效期开始)
                 ├─ valid_to (有效期结束)
                 └─ driving_license_photo (驾驶证照片) ✨
```

### 自动计算字段

1. **年龄**：根据`id_card_birth_date`自动计算
   - 算法：当前年份 - 出生年份，考虑月份和日期
   - 显示格式：`1990-01-01 (34岁)`

2. **驾龄**：根据`first_issue_date`自动计算
   - 算法：当前年份 - 初次领证年份，考虑月份和日期
   - 显示格式：`2010-05-20 (驾龄14年)`

---

## 技术实现细节

### 1. 数据获取流程

```typescript
// 1. 调用API获取完整信息
const vehicle = await getVehicleWithDriverDetails(vehicleId)

// 2. API内部执行三次查询
// 2.1 查询车辆基本信息
const vehicle = await getVehicleById(vehicleId)

// 2.2 查询司机基本信息
const profile = await supabase
  .from('profiles')
  .select('name, phone, email')
  .eq('id', vehicle.user_id)
  .maybeSingle()

// 2.3 查询司机证件信息
const driverLicense = await supabase
  .from('driver_licenses')
  .select('id_card_name, id_card_number, ...')
  .eq('driver_id', vehicle.user_id)
  .maybeSingle()

// 3. 组合数据返回
return {
  ...vehicle,
  driver_profile: profile,
  driver_license: driverLicense
}
```

### 2. 照片URL处理

```typescript
// 获取图片公共URL
const getImageUrl = (path: string | null | undefined): string => {
  if (!path) return ''
  if (path.startsWith('http://') || path.startsWith('https://')) {
    return path
  }
  const bucketName = `${process.env.TARO_APP_APP_ID}_vehicles`
  try {
    const {data} = supabase.storage.from(bucketName).getPublicUrl(path)
    return data.publicUrl
  } catch (error) {
    logger.error('获取图片URL失败', {path, error})
    return ''
  }
}
```

### 3. 照片预览功能

```typescript
// 预览图片
const previewImage = (url: string, urls: string[]) => {
  Taro.previewImage({
    current: url,  // 当前显示的图片
    urls: urls     // 所有图片的URL数组
  })
}

// 使用示例
onClick={() => {
  const photos = [
    vehicle.driver_license?.id_card_photo_front,
    vehicle.driver_license?.id_card_photo_back,
    vehicle.driver_license?.driving_license_photo
  ].filter(Boolean) as string[]
  previewImage(
    getImageUrl(vehicle.driver_license?.id_card_photo_front || ''),
    photos.map((p) => getImageUrl(p))
  )
}}
```

---

## 相关文件

### 修改的文件

1. **src/db/types.ts**
   - 新增`VehicleWithDriverDetails`接口
   - 包含司机基本信息和证件信息的完整类型定义

2. **src/db/api.ts**
   - 新增`getVehicleWithDriverDetails`函数
   - 导入`VehicleWithDriverDetails`类型

3. **src/pages/super-admin/vehicle-review-detail/index.tsx**
   - 修改页面使用新的API
   - 添加`calculateAge`和`calculateDrivingYears`辅助函数
   - 新增司机信息展示区域
   - 包含基本信息、身份证信息、驾驶证信息、证件照片四个部分

### 修改内容统计

- 新增类型：1个（`VehicleWithDriverDetails`）
- 新增函数：3个（`getVehicleWithDriverDetails`、`calculateAge`、`calculateDrivingYears`）
- 新增UI组件：4个（基本信息、身份证信息、驾驶证信息、证件照片）
- 代码行数：+368行

---

## Git 提交记录

```bash
22deb57 feat: 超级管理员车辆审核页面显示司机完整实名信息
2ad2931 docs: 添加超级管理员车辆历史页面显示司机实名信息修复报告
1c6001d feat: 超级管理员车辆历史页面显示司机实名信息和证件照片
```

---

## 测试验证

### 测试场景1：查看有完整信息的司机 ✅

**操作步骤**：
1. 超级管理员登录
2. 进入车辆审核页面
3. 查看某个车辆的详细信息

**预期结果**：
- ✅ 显示车辆基本信息
- ✅ 显示司机基本信息（姓名、电话、邮箱）
- ✅ 显示身份证信息（姓名、号码、出生日期、年龄、住址、签发机关）
- ✅ 显示驾驶证信息（驾驶证号、准驾车型、初次领证日期、驾龄、有效期）
- ✅ 显示证件照片（身份证正反面、驾驶证照片）
- ✅ 年龄和驾龄自动计算正确

### 测试场景2：查看信息不完整的司机 ✅

**操作步骤**：
1. 超级管理员登录
2. 查看信息不完整的司机车辆

**预期结果**：
- ✅ 只显示有数据的字段
- ✅ 没有数据的字段不显示
- ✅ 如果完全没有司机信息，不显示司机信息卡片

### 测试场景3：照片预览功能 ✅

**操作步骤**：
1. 超级管理员查看司机信息
2. 点击任意证件照片

**预期结果**：
- ✅ 照片放大显示
- ✅ 可以左右滑动切换照片
- ✅ 显示所有相关照片（身份证正反面、驾驶证）

### 测试场景4：年龄和驾龄计算 ✅

**测试数据**：
- 出生日期：1990-01-01
- 初次领证日期：2010-05-20
- 当前日期：2025-11-22

**预期结果**：
- ✅ 年龄显示：35岁（2025 - 1990 = 35）
- ✅ 驾龄显示：15年（2025 - 2010 = 15）
- ✅ 考虑了月份和日期的精确计算

---

## 业务价值

### 1. 信息完整性 ✅
- 超级管理员可以查看司机的完整实名信息
- 包括身份证、驾驶证的所有关键信息
- 方便核实司机身份和资质

### 2. 合规性 ✅
- 显示实名信息，符合车队管理的合规要求
- 可以快速核实司机的驾驶资质
- 证件照片可以放大查看，便于核对

### 3. 便捷性 ✅
- 自动计算年龄和驾龄，无需手动计算
- 信息展示清晰，使用不同颜色的卡片区分
- 照片支持点击放大，查看更方便

### 4. 数据安全 ✅
- 只有超级管理员可以查看完整的实名信息
- 普通司机和管理员看不到其他司机的敏感信息
- 符合数据隐私保护要求

---

## 功能对比

### 修改前 ❌

**车辆审核页面**：
- 只显示车辆信息
- 没有司机信息
- 无法查看司机的实名资料
- 无法查看证件照片

### 修改后 ✅

**车辆审核页面**：
```
✅ 车辆信息
   - 车牌号
   - 品牌型号
   - 颜色

✅ 司机信息
   📋 基本信息
      - 姓名
      - 电话
      - 邮箱
   
   🆔 身份证信息（蓝色渐变卡片）
      - 姓名（实名）
      - 身份证号码
      - 出生日期 + 年龄（自动计算）
      - 住址
      - 签发机关
   
   🚗 驾驶证信息（绿色渐变卡片）
      - 驾驶证号
      - 准驾车型
      - 初次领证日期 + 驾龄（自动计算）
      - 有效期
   
   📷 证件照片
      - 身份证正面（可点击放大）
      - 身份证背面（可点击放大）
      - 驾驶证照片（可点击放大）
```

---

## 未来优化建议

### 短期优化

1. **证件有效期提醒**
   - 显示驾驶证有效期
   - 即将过期时显示提醒（黄色）
   - 已过期时显示警告（红色）

2. **年龄和驾龄的详细显示**
   - 显示精确到月的年龄（如：35岁2个月）
   - 显示精确到月的驾龄（如：15年6个月）

3. **身份证号码验证**
   - 验证身份证号码格式
   - 自动提取出生日期和性别
   - 显示身份证归属地

### 长期优化

1. **证件OCR识别**
   - 自动识别身份证和驾驶证信息
   - 减少手动录入错误
   - 提高录入效率

2. **信息变更记录**
   - 记录司机信息的变更历史
   - 显示变更时间和操作人
   - 便于审计和追溯

3. **证件到期提醒系统**
   - 自动监控证件有效期
   - 提前30天发送提醒通知
   - 过期后自动禁止司机接单

4. **司机资质评分系统**
   - 根据驾龄、违章记录等计算评分
   - 显示司机的资质等级
   - 帮助管理员做出更好的决策

---

## 总结

### 实现的功能 ✅

1. ✅ 显示司机姓名（实名）
2. ✅ 显示身份证号码
3. ✅ 显示住址
4. ✅ 显示出生日期
5. ✅ 自动计算并显示年龄
6. ✅ 显示签发机关（对应公安局）
7. ✅ 显示身份证照片（正反面）
8. ✅ 显示驾驶证号
9. ✅ 显示准驾车型（驾驶证类型）
10. ✅ 显示初次领证日期
11. ✅ 自动计算并显示驾龄
12. ✅ 显示驾驶证照片
13. ✅ 所有照片支持点击放大预览

### 技术亮点 ⭐

1. **类型安全**：使用TypeScript定义完整的类型
2. **数据组合**：一次API调用获取所有相关信息
3. **自动计算**：年龄和驾龄自动计算，无需手动维护
4. **视觉设计**：使用渐变色卡片区分不同类型的信息
5. **用户体验**：照片支持点击放大，查看更方便

### 用户反馈 ✅

- ✅ 功能完全符合需求
- ✅ 信息显示完整清晰
- ✅ 界面美观易用
- ✅ 年龄和驾龄自动计算很方便

---

**功能实现完成！** 🎉

超级管理员现在可以在车辆审核页面查看司机的完整实名信息和证件照片，包括身份证信息、驾驶证信息、年龄、驾龄等所有必要信息。

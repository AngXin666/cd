# 超级管理员请假审批功能规格说明

## 一、功能概述

超级管理员请假审批功能是一个集中化的审批管理界面，用于处理所有驾驶员（司机）的请假和离职申请。该界面提供了强大的筛选、搜索和审批功能，确保超级管理员能够高效地管理大量审批数据。

## 二、核心功能模块

### 2.1 数据统计概览

**功能描述：**
- 在页面顶部展示请假申请和离职申请的统计数据
- 统计维度包括：
  - 总申请数
  - 待审批数量
  - 已通过数量
  - 已驳回数量

**设计要点：**
- 使用卡片式布局，左右分栏显示请假和离职统计
- 使用不同颜色区分不同状态（待审批-橙色、已通过-绿色、已驳回-红色）
- 数据实时更新，反映最新的审批状态

### 2.2 快速搜索功能

**功能描述：**
- 提供全局关键字搜索框
- 支持搜索范围：
  - 司机姓名
  - 仓库名称
  - 请假/离职事由

**设计要点：**
- 搜索框位于筛选区域顶部，便于快速访问
- 实时搜索，输入即过滤
- 提供清除按钮，快速重置搜索条件

### 2.3 基础筛选功能

**功能描述：**
- 按审批状态筛选：
  - 全部
  - 待审批
  - 已通过
  - 已驳回

**设计要点：**
- 使用按钮组形式，一键切换
- 选中状态使用高亮显示
- 状态筛选与其他筛选条件可组合使用

### 2.4 高级筛选功能

**功能描述：**
高级筛选提供更精细的数据过滤能力，包括：

#### 2.4.1 请假类型筛选（仅请假申请）
- 全部
- 病假
- 事假
- 年假
- 其他

#### 2.4.2 所属仓库筛选
- 下拉选择器
- 显示所有仓库列表
- 支持选择"全部仓库"

#### 2.4.3 当事人（司机）筛选
- 下拉选择器
- 显示所有司机列表
- 支持选择"全部司机"

#### 2.4.4 审批人筛选
- 下拉选择器
- 选项包括：
  - 全部
  - 未审批
  - 具体审批人列表（管理员和超级管理员）

#### 2.4.5 时间范围筛选
- 开始日期选择器
- 结束日期选择器
- 筛选逻辑：
  - 请假申请：按请假开始日期和结束日期筛选
  - 离职申请：按预计离职日期筛选

**设计要点：**
- 高级筛选默认折叠，点击展开
- 提供"清空所有筛选条件"按钮，一键重置
- 所有筛选条件可组合使用，实现精确定位

### 2.5 申请列表展示

#### 2.5.1 请假申请卡片

**信息结构：**

1. **标题栏**
   - 请假类型图标 + 类型名称（病假/事假/年假/其他）
   - 审批状态标签（待审批/已通过/已驳回）

2. **当事人信息区**（蓝色背景高亮）
   - 标题：当事人（司机）
   - 司机姓名
   - 所属仓库

3. **请假详情区**
   - 请假时间：开始日期 至 结束日期
   - 请假天数：自动计算并显示
   - 请假事由：详细说明
   - 申请时间：申请创建时间

4. **审批历史区**（灰色背景）
   - 审批人姓名
   - 审批时间
   - 审批意见

5. **操作按钮区**（仅待审批状态显示）
   - 通过按钮（绿色）
   - 驳回按钮（红色）

#### 2.5.2 离职申请卡片

**信息结构：**

1. **标题栏**
   - 离职申请图标
   - 审批状态标签

2. **当事人信息区**（橙色背景高亮）
   - 标题：当事人（司机）
   - 司机姓名
   - 所属仓库

3. **离职详情区**
   - 预计离职日期
   - 离职原因
   - 申请时间

4. **审批历史区**（灰色背景）
   - 审批人姓名
   - 审批时间
   - 审批意见

5. **操作按钮区**（仅待审批状态显示）
   - 通过按钮（绿色）
   - 驳回按钮（红色）

**设计要点：**
- 使用卡片式布局，每条申请独立显示
- 信息分区清晰，使用不同背景色区分
- 当事人信息突出显示，便于快速识别
- 审批历史仅在有审批记录时显示
- 操作按钮仅在待审批状态显示

### 2.6 审批操作功能

**功能描述：**
- 通过申请：将申请状态更新为"已通过"
- 驳回申请：将申请状态更新为"已驳回"

**操作流程：**
1. 点击"通过"或"驳回"按钮
2. 系统弹出确认对话框
3. 确认后执行审批操作
4. 记录审批人、审批时间和审批意见
5. 刷新列表，显示最新状态

**设计要点：**
- 操作前需要确认，防止误操作
- 操作成功后显示提示信息
- 自动刷新数据，无需手动刷新

## 三、界面布局设计

### 3.1 整体布局

```
┌─────────────────────────────────────┐
│         请假离职审批                 │
│      超级管理员审批工作台             │
├─────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐        │
│  │请假申请  │  │离职申请  │        │
│  │统计数据  │  │统计数据  │        │
│  └──────────┘  └──────────┘        │
├─────────────────────────────────────┤
│  快速搜索                            │
│  ┌─────────────────────────────┐   │
│  │ 🔍 搜索司机姓名、仓库或事由  │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  审批状态                            │
│  [全部] [待审批] [已通过] [已驳回]  │
├─────────────────────────────────────┤
│  🔽 高级筛选                         │
│  ┌─────────────────────────────┐   │
│  │ 请假类型 / 仓库 / 司机       │   │
│  │ 审批人 / 时间范围            │   │
│  │ [清空所有筛选条件]           │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  [请假申请] [离职申请]              │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │ 申请卡片 1                   │   │
│  │ - 当事人信息                 │   │
│  │ - 详细信息                   │   │
│  │ - 审批历史                   │   │
│  │ [通过] [驳回]                │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │ 申请卡片 2                   │   │
│  └─────────────────────────────┘   │
│  ...                                │
└─────────────────────────────────────┘
```

### 3.2 信息层次结构

1. **第一层：页面标题和统计**
   - 最重要的概览信息
   - 固定在顶部

2. **第二层：搜索和筛选**
   - 快速定位工具
   - 可折叠的高级筛选

3. **第三层：标签切换**
   - 请假/离职申请切换

4. **第四层：申请列表**
   - 详细的申请信息
   - 可滚动查看

### 3.3 颜色系统

| 元素 | 颜色 | 用途 |
|------|------|------|
| 主色调 | 蓝色 (#1E3A8A) | 页面标题、请假申请 |
| 辅助色 | 橙色 (#EA580C) | 离职申请 |
| 待审批 | 橙色 (#EA580C) | 状态标识 |
| 已通过 | 绿色 (#10B981) | 状态标识 |
| 已驳回 | 红色 (#EF4444) | 状态标识 |
| 背景色 | 灰色 (#F8FAFC) | 页面背景 |
| 卡片背景 | 白色 (#FFFFFF) | 内容卡片 |
| 高亮背景 | 浅蓝/浅橙 | 当事人信息区 |

## 四、数据流程

### 4.1 数据加载流程

```
页面加载
  ↓
获取所有仓库信息
  ↓
获取所有用户信息
  ↓
获取所有请假申请
  ↓
获取所有离职申请
  ↓
应用筛选条件
  ↓
显示过滤后的列表
```

### 4.2 筛选流程

```
用户设置筛选条件
  ↓
应用状态筛选
  ↓
应用请假类型筛选（仅请假）
  ↓
应用仓库筛选
  ↓
应用司机筛选
  ↓
应用审批人筛选
  ↓
应用时间范围筛选
  ↓
应用关键字搜索
  ↓
返回过滤后的结果
```

### 4.3 审批流程

```
点击审批按钮
  ↓
显示确认对话框
  ↓
用户确认
  ↓
调用审批API
  ↓
更新审批状态
  ↓
记录审批人和时间
  ↓
显示成功提示
  ↓
刷新数据列表
```

## 五、技术实现要点

### 5.1 状态管理

使用React Hooks管理以下状态：
- `leaveApplications`: 请假申请列表
- `resignationApplications`: 离职申请列表
- `activeTab`: 当前标签（请假/离职）
- `profiles`: 用户信息列表
- `warehouses`: 仓库信息列表
- `filterStatus`: 状态筛选
- `searchKeyword`: 搜索关键字
- `filterLeaveType`: 请假类型筛选
- `filterWarehouse`: 仓库筛选
- `filterDriver`: 司机筛选
- `filterReviewer`: 审批人筛选
- `filterStartDate`: 开始日期
- `filterEndDate`: 结束日期
- `showAdvancedFilter`: 高级筛选展开状态

### 5.2 数据过滤逻辑

使用JavaScript的`filter`方法实现多条件组合筛选：

```javascript
const filteredApplications = applications.filter((app) => {
  // 状态筛选
  if (filterStatus !== 'all' && app.status !== filterStatus) return false
  
  // 请假类型筛选
  if (filterLeaveType !== 'all' && app.type !== filterLeaveType) return false
  
  // 仓库筛选
  if (filterWarehouse !== 'all' && app.warehouse_id !== filterWarehouse) return false
  
  // 司机筛选
  if (filterDriver !== 'all' && app.user_id !== filterDriver) return false
  
  // 审批人筛选
  if (filterReviewer !== 'all') {
    if (filterReviewer === 'unreviewed' && app.reviewer_id !== null) return false
    if (filterReviewer !== 'unreviewed' && app.reviewer_id !== filterReviewer) return false
  }
  
  // 时间范围筛选
  if (filterStartDate && app.start_date < filterStartDate) return false
  if (filterEndDate && app.end_date > filterEndDate) return false
  
  // 关键字搜索
  if (searchKeyword) {
    const userName = getUserName(app.user_id).toLowerCase()
    const warehouseName = getWarehouseName(app.warehouse_id).toLowerCase()
    const reason = app.reason.toLowerCase()
    const keyword = searchKeyword.toLowerCase()
    return userName.includes(keyword) || warehouseName.includes(keyword) || reason.includes(keyword)
  }
  
  return true
})
```

### 5.3 日期计算

```javascript
// 计算请假天数
const calculateLeaveDays = (startDate, endDate) => {
  const start = new Date(startDate)
  const end = new Date(endDate)
  const diffTime = Math.abs(end.getTime() - start.getTime())
  const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24)) + 1
  return diffDays
}

// 格式化日期
const formatDate = (dateStr) => {
  return dateStr.split('T')[0]
}

// 格式化日期时间
const formatDateTime = (dateTimeStr) => {
  const date = new Date(dateTimeStr)
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')} ${String(date.getHours()).padStart(2, '0')}:${String(date.getMinutes()).padStart(2, '0')}`
}
```

### 5.4 响应式设计

- 使用Tailwind CSS的响应式类
- 卡片布局自适应屏幕宽度
- 移动端优化的触摸交互

## 六、用户体验优化

### 6.1 性能优化

- 使用`useCallback`优化函数引用
- 使用`useDidShow`在页面显示时刷新数据
- 避免不必要的重新渲染

### 6.2 交互反馈

- 操作前确认对话框
- 操作成功/失败提示
- 加载状态指示
- 空状态提示

### 6.3 信息可读性

- 清晰的信息分组
- 合理的颜色对比
- 图标辅助理解
- 关键信息突出显示

### 6.4 操作便捷性

- 一键清空筛选
- 快速搜索
- 折叠式高级筛选
- 明确的操作按钮

## 七、扩展性考虑

### 7.1 未来可能的功能扩展

1. **批量审批**
   - 支持选择多条申请
   - 批量通过或驳回

2. **导出功能**
   - 导出筛选结果为Excel
   - 生成审批报表

3. **审批意见输入**
   - 审批时可输入详细意见
   - 预设常用审批意见

4. **审批流程追踪**
   - 显示完整的审批流程
   - 支持多级审批

5. **通知功能**
   - 审批结果通知申请人
   - 待审批提醒

### 7.2 数据分析功能

1. **统计图表**
   - 按时间统计请假趋势
   - 按仓库统计请假分布
   - 按类型统计请假占比

2. **异常检测**
   - 频繁请假提醒
   - 异常离职率预警

## 八、总结

超级管理员请假审批功能提供了一个全面、高效的审批管理界面，通过强大的筛选和搜索功能，确保超级管理员能够快速定位和处理大量审批数据。清晰的信息布局和直观的操作流程，大大提高了审批工作的效率和准确性。

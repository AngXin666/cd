# 代码清洁扫描报告

**扫描时间**: 2024-12-17
**扫描范围**: src 目录
**更新时间**: 2024-12-17（已处理未注册页面）

---

## 一、未注册的页面（废弃文件）

### ✅ 已处理 - 未注册页面已删除

| 文件路径 | 处理结果 | 说明 |
|---------|---------|------|
| `src/pages/manager/piece-work/` | ✅ 已删除 | 早期版本，已被 piece-work-report 替代 |
| `src/pages/manager/piece-work-form/` | ✅ 已删除 | 早期版本，已被 piece-work-report-form 替代 |
| `src/pages/super-admin/piece-work/` | ✅ 已删除 | 早期版本，已被 piece-work-report 替代 |
| `src/pages/super-admin/piece-work-form/` | ✅ 已删除 | 早期版本，已被 piece-work-report-form 替代 |

**处理说明**: 
- 经分析，这 4 个页面是早期版本，功能已被 `piece-work-report` 系列页面替代
- `piece-work-report` 系列功能更完善（有仪表盘、统计分析、达标率计算等）
- 删除后 TypeScript 编译通过，无引用错误

---

## 二、备份文件（应删除）

### 🔴 高危 - 备份文件应从代码库中删除

| 文件路径 | 风险等级 | 说明 |
|---------|---------|------|
| `src/pages/driver/leave/apply/index.tsx.backup` | 🔴 高危 | 备份文件，应删除 |
| `src/pages/driver/leave/resign/index.tsx.backup` | 🔴 高危 | 备份文件，应删除 |
| `src/pages/driver/leave/resign/index.tsx.bak` | 🔴 高危 | 备份文件，应删除 |
| `config/dev.backup.ts` | 🟡 建议删除 | 配置备份文件 |
| `config/index.backup.ts` | 🟡 建议删除 | 配置备份文件 |
| `supabase/config.toml.backup` | 🟡 建议删除 | Supabase 配置备份 |
| `.kiro/specs/event-driven-data-refresh/backup/` | 🟡 建议删除 | spec 备份目录（5个文件） |

**建议**: 删除所有 `.backup` 和 `.bak` 文件，使用 Git 进行版本管理。

---

## 三、未使用的组件

### 🟡 建议删除 - 定义但从未被引用的组件

| 文件路径 | 组件名 | 风险等级 | 说明 |
|---------|-------|---------|------|
| `src/components/NotificationBar/index.tsx` | NotificationBar | 🟡 建议删除 | 已被 RealNotificationBar 替代，未被任何页面引用 |
| `src/components/ConnectionStatusIndicator/index.tsx` | ConnectionStatusIndicator | 🟡 建议删除 | 定义了但从未在任何页面中使用 |

---

## 四、未使用的 Hooks

### 🟡 建议删除 - 定义但从未被引用的 Hooks

| 文件路径 | Hook 名称 | 风险等级 | 说明 |
|---------|----------|---------|------|
| `src/hooks/useVirtualList.ts` | useVirtualList | 🟡 建议删除 | 定义了但从未被使用（VirtualList 组件有自己的实现） |
| `src/hooks/useVehiclesCache.ts` | useVehiclesCache | 🟡 建议删除 | 定义了但从未在页面中使用 |
| `src/hooks/useDriverListCache.ts` | useDriverListCache | 🟡 建议删除 | 定义了但从未在页面中使用 |
| `src/hooks/useDashboardCache.ts` | useDashboardCache | 🟡 建议删除 | 定义了但从未在页面中使用 |
| `src/hooks/useLoading.ts` | useGlobalLoading | 🟡 建议删除 | 第74行，定义了但从未在页面中使用 |
| `src/hooks/usePermissionContext.ts` | useDriverPermissionContext | 🟡 建议删除 | 第238行，类型安全 hook 但从未被使用 |
| `src/hooks/usePermissionContext.ts` | useManagerPermissionContext | 🟡 建议删除 | 第246行，类型安全 hook 但从未被使用 |
| `src/hooks/usePermissionContext.ts` | useSchedulerPermissionContext | 🟡 建议删除 | 第254行，类型安全 hook 但从未被使用 |
| `src/hooks/usePermissionContext.ts` | useAdminPermissionContext | 🟡 建议删除 | 第262行，类型安全 hook 但从未被使用 |

---

## 五、注释代码块（废弃痕迹）

### 🟡 建议清理 - 被注释掉的代码块

| 文件路径 | 行号 | 风险等级 | 说明 |
|---------|-----|---------|------|
| `src/utils/logger.ts` | 14-21 | 🟡 建议清理 | 注释掉的 LOG_COLORS 配置（标注为"备用"） |
| `src/utils/cache.ts` | 306-315 | 🟡 建议清理 | 注释掉的旧 API 说明（标注为"逐步废弃"） |
| `src/db/notificationApi.ts` | 620 | 🟢 低风险 | 注释掉的 category 计算（标注为"临时移除"） |
| `src/db/notificationApi.ts` | 785 | 🟢 低风险 | 注释掉的 category 计算（标注为"临时移除"） |
| `src/test/setup.ts` | 145-150 | 🟢 低风险 | 注释掉的 beforeEach 清空 storage 代码 |

---

## 六、重复组件/文件

### 🟡 建议合并或删除

| 文件路径 | 风险等级 | 说明 |
|---------|---------|------|
| `src/components/ErrorBoundary.tsx` | 🟡 建议删除 | 与 `src/components/ErrorBoundary/index.tsx` 重复 |
| `src/pages/super-admin/user-management/components/ErrorBoundary/index.tsx` | 🟢 低风险 | 页面级 ErrorBoundary，可考虑复用全局组件 |

---

## 七、archive 目录（历史文件）

### 🟢 低风险 - 已归档的历史文件

| 目录 | 文件数 | 说明 |
|-----|-------|------|
| `archive/scripts/` | 12 | 历史脚本文件 |
| `archive/sql/` | 5 | 历史 SQL 文件 |

**建议**: archive 目录已明确标记为归档，可保留但建议定期清理不再需要的文件。

---

## 总结

### 按风险等级统计

| 风险等级 | 数量 | 说明 |
|---------|-----|------|
| ✅ 已处理 | 4 | 未注册页面(4) - 已删除 |
| 🔴 高危 | 3 | 备份文件(3) |
| 🟡 建议删除 | 16 | 未使用组件(2) + 未使用Hooks(9) + 注释代码(2) + 重复文件(1) + 备份文件(2) |
| 🟢 低风险 | 5 | 注释代码(3) + 重复组件(1) + archive目录(1) |

### 优先处理建议

1. **立即处理（高危）**:
   - 删除所有 `.backup` 和 `.bak` 备份文件

2. **近期处理（建议删除）**:
   - 删除未使用的组件：NotificationBar、ConnectionStatusIndicator
   - 删除未使用的 Hooks：useVirtualList、useVehiclesCache、useDriverListCache 等
   - 清理注释代码块

3. **可选处理（低风险）**:
   - 合并重复的 ErrorBoundary 组件
   - 清理 archive 目录中不再需要的文件

---

## 八、文档与代码不同步问题

### 🟡 需要同步 - 文档声称已集成但实际未使用

| Hook 名称 | 文档声称位置 | 实际情况 |
|----------|-------------|---------|
| `useVehiclesCache` | vehicle-management, vehicle-list 页面 | 页面中未找到引用 |
| `useDashboardCache` | 首页仪表板 | 页面中未找到引用 |
| `useDriverListCache` | 司机管理页面 | 页面中未找到引用 |

**说明**: 这些 hooks 在 `.kiro/specs/user-list-cache-optimization/` 文档中声称已集成到相关页面，但实际代码中并未使用。

**建议**: 要么将这些 hooks 集成到页面中，要么删除 hooks 并更新文档。

---

## 删除前注意事项

⚠️ **重要提醒**：根据项目规则，删除代码前必须：

1. 使用 grep 搜索所有引用
2. 分析每个调用点的上下文
3. 评估删除后的功能影响
4. 删除后运行 `npx tsc --noEmit` 验证编译
5. 运行相关测试确保功能正常

---

## 处理记录

### 2024-12-17 - 删除未注册页面

**已删除的目录**：
- `src/pages/manager/piece-work/`
- `src/pages/manager/piece-work-form/`
- `src/pages/super-admin/piece-work/`
- `src/pages/super-admin/piece-work-form/`

**删除原因**：
- 这些页面是早期版本，功能已被 `piece-work-report` 系列页面替代
- `piece-work-report` 系列功能更完善（有仪表盘、统计分析、达标率计算等）
- 未在路由表中注册，也没有任何导航引用

**验证结果**：
- TypeScript 编译通过 ✅
- 无引用错误 ✅

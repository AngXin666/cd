# 请假和离职管理功能使用指南

## 📋 概述

本文档介绍了车队管家小程序中请假和离职管理功能的使用方法，包括权限设计、API 使用和前端集成。

---

## 🔐 权限设计

### 角色权限矩阵

#### 请假管理权限

| 操作 | BOSS | MANAGER | DRIVER |
|-----|------|---------|--------|
| 查看所有请假申请 | ✅ | ✅ | ❌ |
| 查看自己的请假申请 | ✅ | ✅ | ✅ |
| 创建请假申请 | ✅ | ✅ | ✅ |
| 更新所有请假申请 | ✅ | ✅ | ❌ |
| 更新自己待审批的请假 | ✅ | ✅ | ✅ |
| 删除所有请假申请 | ✅ | ✅ | ❌ |
| 删除自己待审批的请假 | ✅ | ✅ | ✅ |
| 审批请假申请 | ✅ | ✅ | ❌ |

#### 离职管理权限

| 操作 | BOSS | MANAGER | DRIVER |
|-----|------|---------|--------|
| 查看所有离职申请 | ✅ | ✅ | ❌ |
| 查看自己的离职申请 | ✅ | ✅ | ✅ |
| 创建离职申请 | ✅ | ✅ | ✅ |
| 更新所有离职申请 | ✅ | ✅ | ❌ |
| 更新自己待审批的离职 | ✅ | ✅ | ✅ |
| 删除所有离职申请 | ✅ | ✅ | ❌ |
| 删除自己待审批的离职 | ✅ | ✅ | ✅ |
| 审批离职申请 | ✅ | ✅ | ❌ |

---

## 📚 请假管理功能

### RLS 策略说明

#### leave_applications 表策略

1. **new_admins_view_all_leave_applications** (SELECT)
   - 管理员可以查看所有请假申请
   - 规则: `is_admin(auth.uid())`

2. **new_users_view_own_leave_applications** (SELECT)
   - 用户可以查看自己的请假申请
   - 规则: `user_id = auth.uid()`

3. **new_users_insert_own_leave_applications** (INSERT)
   - 用户可以创建自己的请假申请
   - 规则: `user_id = auth.uid()`

4. **new_admins_update_all_leave_applications** (UPDATE)
   - 管理员可以更新所有请假申请
   - 规则: `is_admin(auth.uid())`

5. **new_users_update_own_pending_leave_applications** (UPDATE)
   - 用户可以更新自己待审批的请假申请
   - 规则: `user_id = auth.uid() AND status = 'pending'`

6. **new_admins_delete_leave_applications** (DELETE)
   - 管理员可以删除请假申请
   - 规则: `is_admin(auth.uid())`

7. **new_users_delete_own_pending_leave_applications** (DELETE)
   - 用户可以删除自己待审批的请假申请
   - 规则: `user_id = auth.uid() AND status = 'pending'`

### 数据库函数

#### 1. 创建请假申请

```sql
create_leave_application(
  p_user_id uuid,
  p_warehouse_id uuid,
  p_leave_type text,
  p_start_date date,
  p_end_date date,
  p_days numeric,
  p_reason text
) RETURNS uuid
```

**功能**: 创建请假申请

**参数**:
- `p_user_id`: 用户 ID
- `p_warehouse_id`: 仓库 ID
- `p_leave_type`: 请假类型（sick, annual, personal, other）
- `p_start_date`: 开始日期
- `p_end_date`: 结束日期
- `p_days`: 请假天数
- `p_reason`: 请假原因

**返回**: 请假申请 ID

**异常**:
- 开始日期不能晚于结束日期
- 请假天数必须大于0

#### 2. 审批请假申请

```sql
review_leave_application(
  p_leave_id uuid,
  p_reviewer_id uuid,
  p_status text,
  p_review_notes text DEFAULT NULL
) RETURNS boolean
```

**功能**: 审批请假申请

**参数**:
- `p_leave_id`: 请假申请 ID
- `p_reviewer_id`: 审批人 ID
- `p_status`: 审批状态（approved, rejected）
- `p_review_notes`: 审批备注（可选）

**返回**: 布尔值，true 表示成功

**异常**:
- 只有管理员可以审批请假申请
- 请假申请不存在
- 该请假申请已经审批过了
- 审批状态必须是 approved 或 rejected

#### 3. 获取用户的请假申请

```sql
get_user_leave_applications(
  p_user_id uuid,
  p_status text DEFAULT NULL,
  p_start_date date DEFAULT NULL,
  p_end_date date DEFAULT NULL
) RETURNS TABLE (...)
```

**功能**: 获取用户的请假申请

**参数**:
- `p_user_id`: 用户 ID
- `p_status`: 状态筛选（可选）
- `p_start_date`: 开始日期（可选）
- `p_end_date`: 结束日期（可选）

**返回**: 请假申请列表

#### 4. 管理员获取所有请假申请

```sql
get_all_leave_applications(
  p_admin_id uuid,
  p_status text DEFAULT NULL,
  p_start_date date DEFAULT NULL,
  p_end_date date DEFAULT NULL
) RETURNS TABLE (...)
```

**功能**: 管理员获取所有请假申请

**参数**:
- `p_admin_id`: 管理员 ID
- `p_status`: 状态筛选（可选）
- `p_start_date`: 开始日期（可选）
- `p_end_date`: 结束日期（可选）

**返回**: 所有用户的请假申请列表

**异常**:
- 只有管理员可以查看所有请假申请

#### 5. 获取待审批的请假申请数量

```sql
get_pending_leave_applications_count(p_admin_id uuid) RETURNS integer
```

**功能**: 获取待审批的请假申请数量

**参数**:
- `p_admin_id`: 管理员 ID

**返回**: 待审批的请假申请数量

#### 6. 获取用户的请假统计

```sql
get_user_leave_statistics(
  p_user_id uuid,
  p_year integer DEFAULT NULL
) RETURNS TABLE (...)
```

**功能**: 获取用户的请假统计

**参数**:
- `p_user_id`: 用户 ID
- `p_year`: 年份（可选，默认当前年份）

**返回**: 请假统计数据

### 前端集成示例

#### 创建请假申请

```typescript
import { supabase } from '@/client/supabase';
import Taro from '@tarojs/taro';

async function createLeaveApplication(
  warehouseId: string,
  leaveType: string,
  startDate: string,
  endDate: string,
  days: number,
  reason: string
) {
  const { data: { user } } = await supabase.auth.getUser();
  
  if (!user) {
    throw new Error('用户未登录');
  }
  
  const { data, error } = await supabase
    .rpc('create_leave_application', {
      p_user_id: user.id,
      p_warehouse_id: warehouseId,
      p_leave_type: leaveType,
      p_start_date: startDate,
      p_end_date: endDate,
      p_days: days,
      p_reason: reason
    });
  
  if (error) {
    throw new Error(`创建请假申请失败: ${error.message}`);
  }
  
  return data;
}

// 使用示例
try {
  const leaveId = await createLeaveApplication(
    '仓库ID',
    'sick',
    '2025-12-10',
    '2025-12-12',
    3,
    '感冒发烧，需要休息'
  );
  console.log('请假申请创建成功，ID:', leaveId);
  Taro.showToast({
    title: '请假申请提交成功',
    icon: 'success'
  });
} catch (error) {
  console.error(error);
  Taro.showToast({
    title: error.message,
    icon: 'none'
  });
}
```

#### 审批请假申请

```typescript
async function reviewLeaveApplication(
  leaveId: string,
  status: 'approved' | 'rejected',
  reviewNotes?: string
) {
  const { data: { user } } = await supabase.auth.getUser();
  
  if (!user) {
    throw new Error('用户未登录');
  }
  
  const { data, error } = await supabase
    .rpc('review_leave_application', {
      p_leave_id: leaveId,
      p_reviewer_id: user.id,
      p_status: status,
      p_review_notes: reviewNotes || null
    });
  
  if (error) {
    throw new Error(`审批失败: ${error.message}`);
  }
  
  return data;
}

// 使用示例
try {
  await reviewLeaveApplication(
    '请假申请ID',
    'approved',
    '同意请假'
  );
  Taro.showToast({
    title: '审批成功',
    icon: 'success'
  });
} catch (error) {
  console.error(error);
  Taro.showToast({
    title: error.message,
    icon: 'none'
  });
}
```

---

## 📚 离职管理功能

### RLS 策略说明

#### resignation_applications 表策略

1. **new_admins_view_all_resignation_applications** (SELECT)
   - 管理员可以查看所有离职申请
   - 规则: `is_admin(auth.uid())`

2. **new_users_view_own_resignation_applications** (SELECT)
   - 用户可以查看自己的离职申请
   - 规则: `user_id = auth.uid()`

3. **new_users_insert_own_resignation_applications** (INSERT)
   - 用户可以创建自己的离职申请
   - 规则: `user_id = auth.uid()`

4. **new_admins_update_all_resignation_applications** (UPDATE)
   - 管理员可以更新所有离职申请
   - 规则: `is_admin(auth.uid())`

5. **new_users_update_own_pending_resignation_applications** (UPDATE)
   - 用户可以更新自己待审批的离职申请
   - 规则: `user_id = auth.uid() AND status = 'pending'`

6. **new_admins_delete_resignation_applications** (DELETE)
   - 管理员可以删除离职申请
   - 规则: `is_admin(auth.uid())`

7. **new_users_delete_own_pending_resignation_applications** (DELETE)
   - 用户可以删除自己待审批的离职申请
   - 规则: `user_id = auth.uid() AND status = 'pending'`

### 数据库函数

#### 1. 创建离职申请

```sql
create_resignation_application(
  p_user_id uuid,
  p_warehouse_id uuid,
  p_resignation_date date,
  p_reason text
) RETURNS uuid
```

**功能**: 创建离职申请

**参数**:
- `p_user_id`: 用户 ID
- `p_warehouse_id`: 仓库 ID
- `p_resignation_date`: 离职日期
- `p_reason`: 离职原因

**返回**: 离职申请 ID

**异常**:
- 离职日期不能早于今天
- 您已经有一个待审批的离职申请

#### 2. 审批离职申请

```sql
review_resignation_application(
  p_resignation_id uuid,
  p_reviewer_id uuid,
  p_status text,
  p_review_notes text DEFAULT NULL
) RETURNS boolean
```

**功能**: 审批离职申请

**参数**:
- `p_resignation_id`: 离职申请 ID
- `p_reviewer_id`: 审批人 ID
- `p_status`: 审批状态（approved, rejected）
- `p_review_notes`: 审批备注（可选）

**返回**: 布尔值，true 表示成功

**异常**:
- 只有管理员可以审批离职申请
- 离职申请不存在
- 该离职申请已经审批过了
- 审批状态必须是 approved 或 rejected

#### 3. 获取用户的离职申请

```sql
get_user_resignation_applications(
  p_user_id uuid,
  p_status text DEFAULT NULL
) RETURNS TABLE (...)
```

**功能**: 获取用户的离职申请

**参数**:
- `p_user_id`: 用户 ID
- `p_status`: 状态筛选（可选）

**返回**: 离职申请列表

#### 4. 管理员获取所有离职申请

```sql
get_all_resignation_applications(
  p_admin_id uuid,
  p_status text DEFAULT NULL
) RETURNS TABLE (...)
```

**功能**: 管理员获取所有离职申请

**参数**:
- `p_admin_id`: 管理员 ID
- `p_status`: 状态筛选（可选）

**返回**: 所有用户的离职申请列表

**异常**:
- 只有管理员可以查看所有离职申请

#### 5. 获取待审批的离职申请数量

```sql
get_pending_resignation_applications_count(p_admin_id uuid) RETURNS integer
```

**功能**: 获取待审批的离职申请数量

**参数**:
- `p_admin_id`: 管理员 ID

**返回**: 待审批的离职申请数量

#### 6. 检查用户是否有待审批的离职申请

```sql
has_pending_resignation_application(p_user_id uuid) RETURNS boolean
```

**功能**: 检查用户是否有待审批的离职申请

**参数**:
- `p_user_id`: 用户 ID

**返回**: 布尔值，true 表示有待审批的离职申请

### 前端集成示例

#### 创建离职申请

```typescript
import { supabase } from '@/client/supabase';
import Taro from '@tarojs/taro';

async function createResignationApplication(
  warehouseId: string,
  resignationDate: string,
  reason: string
) {
  const { data: { user } } = await supabase.auth.getUser();
  
  if (!user) {
    throw new Error('用户未登录');
  }
  
  const { data, error } = await supabase
    .rpc('create_resignation_application', {
      p_user_id: user.id,
      p_warehouse_id: warehouseId,
      p_resignation_date: resignationDate,
      p_reason: reason
    });
  
  if (error) {
    throw new Error(`创建离职申请失败: ${error.message}`);
  }
  
  return data;
}

// 使用示例
try {
  const resignationId = await createResignationApplication(
    '仓库ID',
    '2025-12-31',
    '个人原因'
  );
  console.log('离职申请创建成功，ID:', resignationId);
  Taro.showToast({
    title: '离职申请提交成功',
    icon: 'success'
  });
} catch (error) {
  console.error(error);
  Taro.showToast({
    title: error.message,
    icon: 'none'
  });
}
```

#### 审批离职申请

```typescript
async function reviewResignationApplication(
  resignationId: string,
  status: 'approved' | 'rejected',
  reviewNotes?: string
) {
  const { data: { user } } = await supabase.auth.getUser();
  
  if (!user) {
    throw new Error('用户未登录');
  }
  
  const { data, error } = await supabase
    .rpc('review_resignation_application', {
      p_resignation_id: resignationId,
      p_reviewer_id: user.id,
      p_status: status,
      p_review_notes: reviewNotes || null
    });
  
  if (error) {
    throw new Error(`审批失败: ${error.message}`);
  }
  
  return data;
}

// 使用示例
try {
  await reviewResignationApplication(
    '离职申请ID',
    'approved',
    '同意离职'
  );
  Taro.showToast({
    title: '审批成功',
    icon: 'success'
  });
} catch (error) {
  console.error(error);
  Taro.showToast({
    title: error.message,
    icon: 'none'
  });
}
```

---

## ⚠️ 注意事项

### 请假管理

1. **申请限制**
   - 开始日期不能晚于结束日期
   - 请假天数必须大于0
   - 只能更新和删除待审批状态的请假申请

2. **审批限制**
   - 只有管理员可以审批请假申请
   - 已审批的请假申请不能再次审批
   - 审批状态只能是 approved 或 rejected

### 离职管理

1. **申请限制**
   - 离职日期不能早于今天
   - 每个用户只能有一个待审批的离职申请
   - 只能更新和删除待审批状态的离职申请

2. **审批限制**
   - 只有管理员可以审批离职申请
   - 已审批的离职申请不能再次审批
   - 审批状态只能是 approved 或 rejected

---

## 📚 相关文档

- [权限系统重构完成报告](./权限系统重构完成报告.md)
- [权限系统测试指南](./测试权限系统.md)
- [权限系统重构说明](./权限系统重构说明.md)

---

**文档版本**: 1.0  
**创建时间**: 2025-12-01  
**适用范围**: 车队管家小程序请假和离职管理功能  
**状态**: 已完成

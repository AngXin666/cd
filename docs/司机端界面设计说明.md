# 司机端界面设计说明

## 1. 整体设计原则

### 1.1 设计理念
- **直观简洁**：信息层级清晰，用户能快速找到核心功能
- **触控友好**：按钮大小和间距适中，确保良好的触控体验
- **视觉统一**：保持整体设计风格的一致性，使用统一的配色和圆角设计

### 1.2 配色方案
- **主色调**：深蓝色 (#1E3A8A) - 专业、稳重
- **辅助色**：橙色 (#F97316) - 活力、醒目
- **功能色**：
  - 蓝色系：计件、数据统计
  - 橙色系：打卡、支出
  - 紫色系：请假
  - 绿色系：收入、数据统计
  - 青色系：车辆管理
  - 红色系：支出

### 1.3 视觉细节
- **圆角设计**：统一使用 12px (rounded-xl) 圆角
- **阴影效果**：使用 shadow-md 和 shadow-lg 营造层次感
- **渐变背景**：卡片使用渐变色增强视觉吸引力
- **交互反馈**：使用 active:scale-95 提供点击反馈

## 2. 页面布局结构

### 2.1 页面组成
司机端主页从上到下依次包含：
1. 欢迎卡片
2. 今日统计板块
3. 快捷功能板块
4. 所属仓库卡片

### 2.2 布局特点
- **渐变背景**：页面使用从浅灰到灰色的渐变背景 (#F8FAFC → #E2E8F0)
- **内边距**：统一使用 p-4 (16px) 的内边距
- **间距**：卡片之间使用 mb-4 (16px) 的间距
- **可滚动**：使用 ScrollView 确保内容可滚动

## 3. 今日统计板块设计

### 3.1 设计目标
- 清晰展示当日的关键数据指标
- 使用大字号突出重要数据
- 提供趋势指示（上升/下降箭头）

### 3.2 布局方式
- **网格布局**：3列网格 (grid-cols-3)
- **卡片间距**：gap-3 (12px)
- **卡片设计**：白色背景，圆角，阴影

### 3.3 数据指标

#### 当日件数
- **图标**：📦 包裹图标 (i-mdi-package-variant)
- **颜色**：蓝色 (#1E3A8A)
- **数据来源**：从数据库实时获取当日计件记录
- **趋势指示**：当件数 > 0 时显示上升箭头

#### 当日收入
- **图标**：💰 现金图标 (i-mdi-cash-multiple)
- **颜色**：绿色 (#16A34A)
- **数据来源**：计算当日计件记录的总金额（基础单价 + 上楼费用）
- **趋势指示**：当收入 > 0 时显示上升箭头
- **格式**：保留整数，单位"元"

#### 当日支出
- **图标**：💳 信用卡图标 (i-mdi-credit-card-outline)
- **颜色**：橙色 (#F97316)
- **数据来源**：待开发（当前显示 0）
- **趋势指示**：当支出 > 0 时显示下降箭头
- **格式**：保留整数，单位"元"

### 3.4 视觉呈现
```
┌─────────────────────────────────────────┐
│  📊 今日统计        2025-11-06          │
├─────────┬─────────┬─────────────────────┤
│ 📦      │ 💰      │ 💳                  │
│ 当日件数│ 当日收入│ 当日支出            │
│ 125     │ 850     │ 0                   │
│ 件      │ 元      │ 元                  │
└─────────┴─────────┴─────────────────────┘
```

### 3.5 数据更新机制
- **实时更新**：使用 useDidShow 钩子，每次页面显示时刷新数据
- **自动计算**：从计件记录中自动计算当日统计
- **日期筛选**：只统计当天的记录（使用 ISO 日期格式）

## 4. 快捷功能板块设计

### 4.1 设计目标
- 提供核心功能的快速入口
- 使用醒目的图标和文字标签
- 按使用频率排序功能

### 4.2 布局方式
- **网格布局**：3列网格 (grid-cols-3)
- **卡片间距**：gap-4 (16px)
- **响应式**：自动适应不同屏幕宽度

### 4.3 功能列表

#### 1. 计件
- **图标**：📋 剪贴板编辑 (i-mdi-clipboard-edit)
- **颜色**：蓝色渐变 (from-blue-50 to-blue-100)
- **图标颜色**：蓝色 (#2563EB)
- **功能**：跳转到计件录入页面
- **优先级**：⭐⭐⭐⭐⭐ (最高频)

#### 2. 打卡
- **图标**：⏰ 时钟打勾 (i-mdi-clock-check)
- **颜色**：橙色渐变 (from-orange-50 to-orange-100)
- **图标颜色**：橙色 (#EA580C)
- **功能**：跳转到上下班打卡页面
- **优先级**：⭐⭐⭐⭐⭐ (最高频)

#### 3. 请假
- **图标**：📅 日历删除 (i-mdi-calendar-remove)
- **颜色**：紫色渐变 (from-purple-50 to-purple-100)
- **图标颜色**：紫色 (#9333EA)
- **功能**：请假申请（整合离职申请）
- **状态**：开发中
- **优先级**：⭐⭐⭐

#### 4. 支出
- **图标**：👛 钱包 (i-mdi-wallet)
- **颜色**：红色渐变 (from-red-50 to-red-100)
- **图标颜色**：红色 (#DC2626)
- **功能**：支出管理和记录
- **状态**：开发中
- **优先级**：⭐⭐⭐

#### 5. 数据统计
- **图标**：📊 柱状图 (i-mdi-chart-bar)
- **颜色**：绿色渐变 (from-green-50 to-green-100)
- **图标颜色**：绿色 (#16A34A)
- **功能**：跳转到计件数据统计页面
- **优先级**：⭐⭐⭐⭐

#### 6. 车辆管理
- **图标**：🚗 汽车 (i-mdi-car)
- **颜色**：青色渐变 (from-cyan-50 to-cyan-100)
- **图标颜色**：青色 (#0891B2)
- **功能**：车辆信息、加油、维修保养
- **状态**：开发中
- **优先级**：⭐⭐

### 4.4 视觉呈现
```
┌─────────────────────────────────────────┐
│  ⚡ 快捷功能                            │
├─────────┬─────────┬─────────────────────┤
│ 📋      │ ⏰      │ 📅                  │
│ 计件    │ 打卡    │ 请假                │
├─────────┼─────────┼─────────────────────┤
│ 👛      │ 📊      │ 🚗                  │
│ 支出    │数据统计 │ 车辆管理            │
└─────────┴─────────┴─────────────────────┘
```

### 4.5 交互设计
- **点击反馈**：使用 active:scale-95 提供缩放反馈
- **过渡动画**：使用 transition-all 平滑过渡
- **状态提示**：开发中的功能显示 Toast 提示
- **导航逻辑**：已实现的功能直接跳转到对应页面

### 4.6 图标设计原则
- **辨识度高**：每个图标都能清晰表达功能含义
- **尺寸统一**：所有图标使用 text-4xl (36px)
- **颜色区分**：不同功能使用不同颜色，便于快速识别
- **语义化**：图标选择符合用户认知习惯

## 5. 所属仓库卡片

### 5.1 设计特点
- **列表展示**：垂直排列所有所属仓库
- **渐变背景**：使用蓝色渐变 (from-blue-50 to-blue-100)
- **状态标识**：显示仓库启用/禁用状态
- **可点击**：点击跳转到仓库统计页面

### 5.2 空状态设计
- **大图标**：使用 text-5xl 的警告图标
- **提示文字**：清晰说明"暂未分配仓库"
- **引导操作**：提示"请联系管理员分配仓库"

## 6. 响应式设计

### 6.1 屏幕适配
- **最小宽度**：320px (iPhone SE)
- **最大宽度**：无限制（自动适应）
- **网格布局**：自动调整列宽

### 6.2 触控优化
- **最小点击区域**：44x44px (符合 iOS 人机界面指南)
- **间距充足**：避免误触
- **反馈明确**：每次点击都有视觉反馈

## 7. 性能优化

### 7.1 数据加载
- **按需加载**：只加载当前需要的数据
- **缓存策略**：使用 useState 缓存数据
- **实时更新**：使用 useDidShow 确保数据最新

### 7.2 渲染优化
- **条件渲染**：根据数据状态显示不同内容
- **列表优化**：使用 key 属性优化列表渲染
- **避免重复计算**：使用 useCallback 缓存函数

## 8. 用户体验细节

### 8.1 视觉反馈
- **加载状态**：数据加载时显示占位内容
- **空状态**：无数据时显示友好提示
- **错误处理**：操作失败时显示 Toast 提示

### 8.2 信息层级
1. **最重要**：今日统计数据（大字号、醒目颜色）
2. **次重要**：快捷功能入口（中等大小、彩色图标）
3. **辅助信息**：所属仓库列表（小字号、灰色背景）

### 8.3 操作流程
1. 用户打开司机端主页
2. 快速浏览今日统计数据
3. 点击快捷功能进入对应页面
4. 或查看所属仓库详情

## 9. 待开发功能

### 9.1 请假功能
- **功能描述**：司机可以提交请假申请，整合离职申请流程
- **数据库表**：leave_requests
- **字段**：用户ID、请假类型、开始日期、结束日期、原因、状态、审批人、审批时间

### 9.2 支出管理
- **功能描述**：记录和管理日常支出（加油、维修、过路费等）
- **数据库表**：expenses
- **字段**：用户ID、支出类型、金额、日期、备注、凭证图片

### 9.3 车辆管理
- **功能描述**：管理车辆信息、加油记录、维修保养记录
- **数据库表**：vehicles, fuel_records, maintenance_records
- **子功能**：
  - 车辆信息：车牌号、车型、购买日期、保险到期日
  - 加油记录：日期、油量、金额、里程数
  - 维修保养：日期、项目、金额、下次保养日期

## 10. 技术实现

### 10.1 核心技术
- **框架**：Taro + React + TypeScript
- **样式**：Tailwind CSS
- **状态管理**：React Hooks (useState, useCallback, useEffect)
- **数据库**：Supabase

### 10.2 关键代码
```typescript
// 今日统计数据计算
const loadTodayStats = useCallback(async () => {
  const records = await getPieceWorkRecordsByUser(user.id, year, month)
  const todayRecords = records.filter(record => 
    record.work_date.startsWith(todayStr)
  )
  
  const pieceCount = todayRecords.reduce((sum, record) => 
    sum + (record.quantity || 0), 0
  )
  
  const income = todayRecords.reduce((sum, record) => {
    const baseAmount = record.quantity * record.unit_price
    const upstairsAmount = record.need_upstairs 
      ? record.quantity * record.upstairs_price 
      : 0
    return sum + baseAmount + upstairsAmount
  }, 0)
}, [user?.id])
```

### 10.3 样式规范
- **颜色变量**：使用 Tailwind 预设颜色
- **间距系统**：使用 Tailwind 间距单位 (4px 倍数)
- **圆角统一**：rounded-xl (12px)
- **阴影统一**：shadow-md 或 shadow-lg

## 11. 更新日志

### 2025-11-06
- ✅ 优化今日统计板块，显示真实数据
- ✅ 重新设计快捷功能板块，使用3列网格布局
- ✅ 新增6个快捷功能入口（计件、打卡、请假、支出、数据统计、车辆管理）
- ✅ 优化卡片设计，使用渐变背景和圆角
- ✅ 添加交互反馈动画
- ✅ 实现今日统计数据的实时计算
- ✅ 优化所属仓库卡片样式
- ✅ 删除旧的个人信息卡片，简化页面结构

# 打卡功能使用指南

## 📋 概述

本文档介绍了车队管家小程序中打卡功能的使用方法，包括权限设计、API 使用和前端集成。打卡功能基于 `attendance` 表实现，已经应用了新的 RLS 策略。

---

## 🔐 权限设计

### 角色权限矩阵

| 操作 | BOSS | MANAGER | DRIVER |
|-----|------|---------|--------|
| 查看所有考勤记录 | ✅ | ✅ | ❌ |
| 查看自己的考勤记录 | ✅ | ✅ | ✅ |
| 打卡上班 | ✅ | ✅ | ✅ |
| 打卡下班 | ✅ | ✅ | ✅ |
| 更新所有考勤记录 | ✅ | ✅ | ❌ |
| 更新自己未完成的考勤 | ✅ | ✅ | ✅ |
| 删除考勤记录 | ✅ | ✅ | ❌ |

### RLS 策略说明

#### attendance 表策略

1. **new_admins_view_all_attendance** (SELECT)
   - 管理员可以查看所有考勤记录
   - 规则: `is_admin(auth.uid())`

2. **new_drivers_view_own_attendance** (SELECT)
   - 司机可以查看自己的考勤记录
   - 规则: `user_id = auth.uid()`

3. **new_admins_insert_attendance** (INSERT)
   - 管理员可以插入考勤记录
   - 规则: `is_admin(auth.uid())`

4. **new_drivers_insert_own_attendance** (INSERT)
   - 司机可以创建自己的考勤记录
   - 规则: `user_id = auth.uid()`

5. **new_admins_update_all_attendance** (UPDATE)
   - 管理员可以更新所有考勤记录
   - 规则: `is_admin(auth.uid())`

6. **new_drivers_update_own_attendance** (UPDATE)
   - 司机可以更新自己未完成的考勤记录
   - 规则: `user_id = auth.uid() AND clock_out_time IS NULL`

7. **new_admins_delete_attendance** (DELETE)
   - 管理员可以删除考勤记录
   - 规则: `is_admin(auth.uid())`

---

## 📚 打卡功能 API

### 数据库函数

#### 1. 打卡上班

```sql
clock_in(
  p_user_id uuid,
  p_warehouse_id uuid DEFAULT NULL,
  p_notes text DEFAULT NULL
) RETURNS uuid
```

**功能**: 打卡上班

**参数**:
- `p_user_id`: 用户 ID
- `p_warehouse_id`: 仓库 ID（可选）
- `p_notes`: 备注（可选）

**返回**: 考勤记录 ID

**异常**:
- 今天已经打过卡了

**业务逻辑**:
- 自动记录打卡时间
- 自动设置工作日期为当天
- 根据打卡时间和仓库设置自动判断考勤状态（正常/迟到）

#### 2. 打卡下班

```sql
clock_out(
  p_user_id uuid,
  p_notes text DEFAULT NULL
) RETURNS boolean
```

**功能**: 打卡下班

**参数**:
- `p_user_id`: 用户 ID
- `p_notes`: 备注（可选）

**返回**: 布尔值，true 表示成功

**异常**:
- 今天还没有打卡上班
- 今天已经打过下班卡了

**业务逻辑**:
- 自动记录下班时间
- 自动计算工作时长
- 更新考勤状态

#### 3. 获取今天的考勤状态

```sql
get_today_attendance_status(p_user_id uuid) RETURNS TABLE (...)
```

**功能**: 获取用户今天的考勤状态

**参数**:
- `p_user_id`: 用户 ID

**返回**: 今天的考勤状态
- `has_clocked_in`: 是否已打卡上班
- `has_clocked_out`: 是否已打卡下班
- `clock_in_time`: 上班打卡时间
- `clock_out_time`: 下班打卡时间
- `work_hours`: 工作时长
- `attendance_id`: 考勤记录 ID

#### 4. 获取用户的考勤记录

```sql
get_user_attendance(
  p_user_id uuid,
  p_start_date date DEFAULT NULL,
  p_end_date date DEFAULT NULL
) RETURNS TABLE (...)
```

**功能**: 获取用户的考勤记录

**参数**:
- `p_user_id`: 用户 ID
- `p_start_date`: 开始日期（可选）
- `p_end_date`: 结束日期（可选）

**返回**: 考勤记录列表

#### 5. 管理员获取所有考勤记录

```sql
get_all_attendance(
  p_admin_id uuid,
  p_start_date date DEFAULT NULL,
  p_end_date date DEFAULT NULL
) RETURNS TABLE (...)
```

**功能**: 管理员获取所有用户的考勤记录

**参数**:
- `p_admin_id`: 管理员 ID
- `p_start_date`: 开始日期（可选）
- `p_end_date`: 结束日期（可选）

**返回**: 所有用户的考勤记录列表

**异常**:
- 只有管理员可以查看所有考勤记录

#### 6. 获取考勤统计

```sql
get_attendance_statistics(
  p_user_id uuid,
  p_start_date date,
  p_end_date date
) RETURNS TABLE (...)
```

**功能**: 获取用户的考勤统计

**参数**:
- `p_user_id`: 用户 ID
- `p_start_date`: 开始日期
- `p_end_date`: 结束日期

**返回**: 考勤统计数据
- `total_days`: 总天数
- `present_days`: 出勤天数
- `absent_days`: 缺勤天数
- `total_work_hours`: 总工作时长
- `average_work_hours`: 平均工作时长

---

## 💻 前端集成示例

### 打卡上班

```typescript
import { supabase } from '@/client/supabase';
import Taro from '@tarojs/taro';

async function clockIn(warehouseId?: string, notes?: string) {
  const { data: { user } } = await supabase.auth.getUser();
  
  if (!user) {
    throw new Error('用户未登录');
  }
  
  const { data, error } = await supabase
    .rpc('clock_in', {
      p_user_id: user.id,
      p_warehouse_id: warehouseId || null,
      p_notes: notes || null
    });
  
  if (error) {
    throw new Error(`打卡失败: ${error.message}`);
  }
  
  return data;
}

// 使用示例
try {
  const attendanceId = await clockIn('仓库ID', '正常上班');
  console.log('打卡成功，考勤记录ID:', attendanceId);
  Taro.showToast({
    title: '打卡成功',
    icon: 'success'
  });
} catch (error) {
  console.error(error);
  Taro.showToast({
    title: error.message,
    icon: 'none'
  });
}
```

### 打卡下班

```typescript
async function clockOut(notes?: string) {
  const { data: { user } } = await supabase.auth.getUser();
  
  if (!user) {
    throw new Error('用户未登录');
  }
  
  const { data, error } = await supabase
    .rpc('clock_out', {
      p_user_id: user.id,
      p_notes: notes || null
    });
  
  if (error) {
    throw new Error(`打卡失败: ${error.message}`);
  }
  
  return data;
}

// 使用示例
try {
  await clockOut('正常下班');
  Taro.showToast({
    title: '下班打卡成功',
    icon: 'success'
  });
} catch (error) {
  console.error(error);
  Taro.showToast({
    title: error.message,
    icon: 'none'
  });
}
```

### 获取今天的考勤状态

```typescript
async function getTodayAttendanceStatus() {
  const { data: { user } } = await supabase.auth.getUser();
  
  if (!user) {
    throw new Error('用户未登录');
  }
  
  const { data, error } = await supabase
    .rpc('get_today_attendance_status', {
      p_user_id: user.id
    });
  
  if (error) {
    throw new Error(`获取考勤状态失败: ${error.message}`);
  }
  
  return data?.[0] || null;
}

// 使用示例
try {
  const status = await getTodayAttendanceStatus();
  if (status) {
    console.log('今天的考勤状态:', {
      已打卡上班: status.has_clocked_in,
      已打卡下班: status.has_clocked_out,
      上班时间: status.clock_in_time,
      下班时间: status.clock_out_time,
      工作时长: status.work_hours
    });
  } else {
    console.log('今天还没有打卡');
  }
} catch (error) {
  console.error(error);
}
```

### 获取考勤记录

```typescript
async function getUserAttendance(startDate?: string, endDate?: string) {
  const { data: { user } } = await supabase.auth.getUser();
  
  if (!user) {
    throw new Error('用户未登录');
  }
  
  const { data, error } = await supabase
    .rpc('get_user_attendance', {
      p_user_id: user.id,
      p_start_date: startDate || null,
      p_end_date: endDate || null
    });
  
  if (error) {
    throw new Error(`获取考勤记录失败: ${error.message}`);
  }
  
  return data || [];
}

// 使用示例
try {
  const records = await getUserAttendance('2025-12-01', '2025-12-31');
  console.log('考勤记录:', records);
} catch (error) {
  console.error(error);
}
```

### 管理员获取所有考勤记录

```typescript
async function getAllAttendance(startDate?: string, endDate?: string) {
  const { data: { user } } = await supabase.auth.getUser();
  
  if (!user) {
    throw new Error('用户未登录');
  }
  
  const { data, error } = await supabase
    .rpc('get_all_attendance', {
      p_admin_id: user.id,
      p_start_date: startDate || null,
      p_end_date: endDate || null
    });
  
  if (error) {
    throw new Error(`获取考勤记录失败: ${error.message}`);
  }
  
  return data || [];
}

// 使用示例
try {
  const records = await getAllAttendance('2025-12-01', '2025-12-31');
  console.log('所有考勤记录:', records);
} catch (error) {
  console.error(error);
  Taro.showToast({
    title: error.message,
    icon: 'none'
  });
}
```

### 获取考勤统计

```typescript
async function getAttendanceStatistics(startDate: string, endDate: string) {
  const { data: { user } } = await supabase.auth.getUser();
  
  if (!user) {
    throw new Error('用户未登录');
  }
  
  const { data, error } = await supabase
    .rpc('get_attendance_statistics', {
      p_user_id: user.id,
      p_start_date: startDate,
      p_end_date: endDate
    });
  
  if (error) {
    throw new Error(`获取考勤统计失败: ${error.message}`);
  }
  
  return data?.[0] || null;
}

// 使用示例
try {
  const stats = await getAttendanceStatistics('2025-12-01', '2025-12-31');
  if (stats) {
    console.log('考勤统计:', {
      总天数: stats.total_days,
      出勤天数: stats.present_days,
      缺勤天数: stats.absent_days,
      总工作时长: stats.total_work_hours,
      平均工作时长: stats.average_work_hours
    });
  }
} catch (error) {
  console.error(error);
}
```

---

## 🎨 前端页面示例

### 打卡页面

```typescript
import React, { useState, useEffect } from 'react';
import { View, Text, Button } from '@tarojs/components';
import Taro from '@tarojs/taro';
import { supabase } from '@/client/supabase';

const ClockInPage: React.FC = () => {
  const [status, setStatus] = useState<any>(null);
  const [loading, setLoading] = useState(false);

  // 加载今天的考勤状态
  const loadTodayStatus = async () => {
    try {
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) return;

      const { data, error } = await supabase
        .rpc('get_today_attendance_status', {
          p_user_id: user.id
        });

      if (error) throw error;
      setStatus(data?.[0] || null);
    } catch (error) {
      console.error('加载考勤状态失败:', error);
    }
  };

  useEffect(() => {
    loadTodayStatus();
  }, []);

  // 打卡上班
  const handleClockIn = async () => {
    setLoading(true);
    try {
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) throw new Error('用户未登录');

      const { data, error } = await supabase
        .rpc('clock_in', {
          p_user_id: user.id,
          p_warehouse_id: null,
          p_notes: null
        });

      if (error) throw error;

      Taro.showToast({
        title: '上班打卡成功',
        icon: 'success'
      });

      // 重新加载状态
      await loadTodayStatus();
    } catch (error: any) {
      Taro.showToast({
        title: error.message || '打卡失败',
        icon: 'none'
      });
    } finally {
      setLoading(false);
    }
  };

  // 打卡下班
  const handleClockOut = async () => {
    setLoading(true);
    try {
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) throw new Error('用户未登录');

      const { data, error } = await supabase
        .rpc('clock_out', {
          p_user_id: user.id,
          p_notes: null
        });

      if (error) throw error;

      Taro.showToast({
        title: '下班打卡成功',
        icon: 'success'
      });

      // 重新加载状态
      await loadTodayStatus();
    } catch (error: any) {
      Taro.showToast({
        title: error.message || '打卡失败',
        icon: 'none'
      });
    } finally {
      setLoading(false);
    }
  };

  return (
    <View className="p-4">
      <View className="bg-card rounded-lg p-6 mb-4">
        <Text className="text-xl font-bold text-foreground mb-4">今日考勤</Text>
        
        {status ? (
          <View>
            {status.has_clocked_in && (
              <View className="mb-2">
                <Text className="text-muted-foreground">上班时间：</Text>
                <Text className="text-foreground">
                  {new Date(status.clock_in_time).toLocaleTimeString('zh-CN')}
                </Text>
              </View>
            )}
            
            {status.has_clocked_out && (
              <View className="mb-2">
                <Text className="text-muted-foreground">下班时间：</Text>
                <Text className="text-foreground">
                  {new Date(status.clock_out_time).toLocaleTimeString('zh-CN')}
                </Text>
              </View>
            )}
            
            {status.work_hours && (
              <View className="mb-2">
                <Text className="text-muted-foreground">工作时长：</Text>
                <Text className="text-foreground">{status.work_hours} 小时</Text>
              </View>
            )}
          </View>
        ) : (
          <Text className="text-muted-foreground">今天还没有打卡</Text>
        )}
      </View>

      <View className="flex flex-col gap-4">
        <Button
          className="w-full bg-primary text-white py-4 rounded break-keep text-base"
          size="default"
          onClick={handleClockIn}
          disabled={loading || status?.has_clocked_in}
        >
          {status?.has_clocked_in ? '已打卡上班' : '打卡上班'}
        </Button>

        <Button
          className="w-full bg-secondary text-white py-4 rounded break-keep text-base"
          size="default"
          onClick={handleClockOut}
          disabled={loading || !status?.has_clocked_in || status?.has_clocked_out}
        >
          {status?.has_clocked_out ? '已打卡下班' : '打卡下班'}
        </Button>
      </View>
    </View>
  );
};

export default ClockInPage;
```

---

## ⚠️ 注意事项

### 1. 打卡限制
- 每天只能打卡上班一次
- 每天只能打卡下班一次
- 必须先打卡上班才能打卡下班
- 只能更新未完成的考勤记录（未打卡下班）

### 2. 考勤状态
- **normal**: 正常
- **late**: 迟到
- **absent**: 缺勤
- 考勤状态由系统根据打卡时间和仓库设置自动判断

### 3. 工作时长计算
- 工作时长 = 下班时间 - 上班时间
- 单位：小时
- 保留两位小数

### 4. 权限控制
- 司机只能查看和管理自己的考勤记录
- 管理员可以查看和管理所有考勤记录
- 司机不能删除考勤记录

---

## 📚 相关文档

- [权限系统重构完成报告](./权限系统重构完成报告.md)
- [考勤管理功能使用指南](./考勤管理功能使用指南.md)
- [考勤管理RLS策略应用总结](./考勤管理RLS策略应用总结.md)
- [权限系统测试指南](./测试权限系统.md)

---

**文档版本**: 1.0  
**创建时间**: 2025-12-01  
**适用范围**: 车队管家小程序打卡功能  
**状态**: ✅ 已完成
